---
phase: 02-library-epub-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - lib/core/db/app_database.g.dart
  - .planning/phases/02-library-epub-import/spike-notes.md
autonomous: true
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
user_setup: []

must_haves:
  truths:
    - "flutter pub get succeeds with drift_dev, file_picker, epubx, html, and receive_sharing_intent_plus in the dependency tree"
    - "dart run build_runner build --delete-conflicting-outputs exits 0 and regenerates app_database.g.dart and router.g.dart"
    - "The epubx spike successfully parses a minimal real EPUB and extracts title/author or documents a verified fallback plan"
    - "The receive_sharing_intent_plus spike resolves on pub.dev and imports without analyzer errors, or a documented fallback is chosen"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Phase 2 dependencies + analyzer override"
      contains: "dependency_overrides"
    - path: "pubspec.yaml"
      provides: "drift_dev in dev_dependencies"
      contains: "drift_dev:"
    - path: ".planning/phases/02-library-epub-import/spike-notes.md"
      provides: "epubx + receive_sharing_intent_plus spike results and go/no-go decision"
  key_links:
    - from: "pubspec.yaml"
      to: "analyzer override"
      via: "dependency_overrides block"
      pattern: "analyzer:\\s*\\^10"
    - from: "pubspec.yaml"
      to: "drift_dev codegen"
      via: "dev_dependencies block"
      pattern: "drift_dev:"
---

<objective>
Unblock Phase 2 by resolving the analyzer conflict, installing Phase 2 dependencies, and de-risking the two assumption-heavy packages (`epubx` Dart 3.11 compat and `receive_sharing_intent_plus` availability) before any schema or parser work begins.

Purpose: The research flagged a CRITICAL BLOCKER — `drift_dev 2.32.1` requires `analyzer >=10` but `riverpod_generator 4.0.3` pins `analyzer ^9`. Phase 1 worked around this by hand-crafting `app_database.g.dart`. Phase 2 needs real generated migrations and cannot proceed until this is resolved. Two further assumptions (A2 and A4 in RESEARCH.md Assumptions Log) must be empirically verified: `epubx` must parse a real EPUB under Dart 3.11, and `receive_sharing_intent_plus` must exist and import cleanly. Each has a documented fallback but the fallback adds hours to scope — we need to know NOW, not mid-parser implementation.
Output: A Phase 2 dependency set that resolves and codegens cleanly, plus written go/no-go evidence for both spikes in `spike-notes.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-library-epub-import/02-CONTEXT.md
@.planning/phases/02-library-epub-import/02-RESEARCH.md
@CLAUDE.md
@pubspec.yaml
@lib/core/db/app_database.dart
@lib/core/db/app_database.g.dart

<interfaces>
<!-- Current pubspec.yaml state (Phase 1 end) -->
<!-- dependencies (current): flutter_riverpod ^3.3.1, riverpod_annotation ^4.0.0, go_router ^17.2.0, drift ^2.32.1, drift_flutter ^0.3.0, shared_preferences ^2.3.0, path_provider ^2.1.5, package_info_plus ^10.0.0 -->
<!-- dev_dependencies (current): build_runner ^2.4.13, riverpod_generator ^4.0.3, riverpod_lint ^3.0.0, flutter_lints ^5.0.0 -->
<!-- NOTE: drift_dev is INTENTIONALLY absent; Phase 2 adds it with analyzer override -->

<!-- Phase 2 adds (from RESEARCH.md "Standard Stack"): -->
<!--   dependencies: file_picker ^11.0.2, epubx ^4.0.0, html ^0.15.5, receive_sharing_intent_plus ^1.6.0 -->
<!--   dev_dependencies: drift_dev ^2.32.1 -->
<!--   dependency_overrides: analyzer ^10.0.0 -->

<!-- Phase 1 hand-crafted app_database.g.dart MUST be deleted before codegen runs, otherwise build_runner will skip (file already exists) or produce conflicts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 dependencies and analyzer override</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml — current exact dependency block (do NOT break Phase 1 deps)
    - .planning/phases/02-library-epub-import/02-RESEARCH.md (Standard Stack section + CRITICAL BLOCKER section)
    - CLAUDE.md (what-NOT-to-use list: do not add epub_view, flutter_html, flutter_widget_from_html, cached_network_image, shimmer, dio)
  </read_first>
  <action>
    Edit `pubspec.yaml` to add Phase 2 dependencies. DO NOT remove or edit any existing Phase 1 dependency.

    Under `dependencies:` add (alphabetical order within the block, after existing entries):
    ```yaml
      epubx: ^4.0.0
      file_picker: ^11.0.2
      html: ^0.15.5
      receive_sharing_intent_plus: ^1.6.0
    ```

    Under `dev_dependencies:` add (after `build_runner`, replacing the explanatory comment block about drift_dev being omitted):
    ```yaml
      drift_dev: ^2.32.1
    ```
    The existing comment block (lines explaining drift_dev is intentionally omitted due to analyzer conflict) MUST be deleted — it is no longer accurate.

    At the top level of `pubspec.yaml` (after `dev_dependencies:` block, before `flutter:` block) add a NEW top-level key:
    ```yaml
    # Phase 2: resolves drift_dev 2.32.1 (requires analyzer >=10) vs
    # riverpod_generator 4.0.3 (pins analyzer ^9) conflict.
    # riverpod_generator uses a subset of analyzer's public API unchanged between v9 and v10,
    # so the override is empirically safe. See riverpod GH #4393, #4684, #4698, #4716.
    dependency_overrides:
      analyzer: ^10.0.0
    ```

    After editing, run from the project root (via mise-managed flutter):
    ```
    flutter pub get
    ```
    Expected: exit 0. A warning about overridden dependencies IS expected and benign — do NOT treat it as failure.

    If `flutter pub get` fails with a version resolution error on `receive_sharing_intent_plus`, proceed to Task 2 which will verify the package exists. Do NOT remove it from pubspec.yaml; capture the exact error text for the spike notes.
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; grep -q "drift_dev:" pubspec.yaml &amp;&amp; grep -q "analyzer: \^10" pubspec.yaml &amp;&amp; grep -q "epubx: \^4.0.0" pubspec.yaml &amp;&amp; grep -q "file_picker: \^11.0.2" pubspec.yaml &amp;&amp; grep -q "html: \^0.15.5" pubspec.yaml &amp;&amp; grep -q "receive_sharing_intent_plus:" pubspec.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` exits 0
    - `pubspec.yaml` contains `drift_dev: ^2.32.1` under `dev_dependencies:`
    - `pubspec.yaml` contains a top-level `dependency_overrides:` block with `analyzer: ^10.0.0`
    - `pubspec.yaml` contains all four new dependencies: `file_picker`, `epubx`, `html`, `receive_sharing_intent_plus`
    - The old `# drift_dev intentionally omitted from Phase 1 — analyzer conflict...` comment block is deleted
    - `pubspec.lock` updated (will be committed in git commit step)
    - No Phase 1 dependency removed (flutter_riverpod, riverpod_annotation, go_router, drift, drift_flutter, shared_preferences, path_provider, package_info_plus all still present)
  </acceptance_criteria>
  <done>Phase 2 deps installed; pubspec.yaml resolves; dependency_overrides block is the SOLE resolution path for the analyzer conflict.</done>
</task>

<task type="auto">
  <name>Task 2: Codegen smoke test + delete hand-crafted app_database.g.dart</name>
  <files>
    lib/core/db/app_database.g.dart
  </files>
  <read_first>
    - lib/core/db/app_database.g.dart (the CURRENT hand-crafted contents — understand what it provides before deletion)
    - lib/core/db/app_database.dart (the @DriftDatabase declaration that will drive regeneration)
    - lib/app/router.dart (uses @Riverpod(keepAlive: true) — must still codegen correctly after analyzer override)
  </read_first>
  <action>
    The Phase 1 `lib/core/db/app_database.g.dart` is hand-crafted (not generated) to work around the drift_dev absence. With drift_dev now installed (Task 1), `build_runner` must regenerate this file. A hand-crafted file already at that path will cause build_runner to either skip it or produce conflicting outputs.

    Step 1: Delete the hand-crafted file:
    ```
    rm lib/core/db/app_database.g.dart
    ```

    Step 2: Run codegen with the conflict-clearing flag:
    ```
    dart run build_runner build --delete-conflicting-outputs
    ```
    Expected outcome: exit 0. The command regenerates `lib/core/db/app_database.g.dart` (drift_dev output), `lib/core/db/app_database_provider.g.dart` (riverpod_generator output — already existed), and `lib/app/router.g.dart` (riverpod_generator output — already existed).

    Step 3: Verify `flutter analyze` passes on the regenerated files:
    ```
    flutter analyze
    ```
    Expected: 0 errors. Info/warning on pre-existing lints from Phase 1 is acceptable, but NO errors.

    Step 4: Verify the test suite still compiles and passes:
    ```
    flutter test
    ```
    Expected: exit 0. Phase 1 tests must still pass after the regeneration.

    If codegen FAILS:
    - If the error mentions `analyzer` version — the dependency_overrides didn't work. Fall back to pinning `drift_dev: 2.18.0` in pubspec.yaml (last version accepting analyzer ^9) and rerun from Task 1. Document the failure in Task 3's `spike-notes.md`.
    - If the error mentions `riverpod_generator` breaking — the override broke riverpod codegen. Same fallback: pin `drift_dev: 2.18.0`.
    - If the error is a Drift schema issue — the existing zero-table schema should codegen trivially; inspect the generated file for diagnostic info.

    Do NOT hand-edit the regenerated `app_database.g.dart` — it must be generator output.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs &amp;&amp; flutter analyze &amp;&amp; flutter test &amp;&amp; test -f lib/core/db/app_database.g.dart &amp;&amp; ! grep -q "HAND-CRAFTED" lib/core/db/app_database.g.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/core/db/app_database.g.dart` exists after regeneration
    - `lib/core/db/app_database.g.dart` does NOT contain the string `HAND-CRAFTED` or any Phase 1 hand-crafted marker comment
    - `lib/core/db/app_database.g.dart` contains generator provenance header (e.g., `// GENERATED CODE - DO NOT MODIFY BY HAND`)
    - `dart run build_runner build --delete-conflicting-outputs` exits 0
    - `flutter analyze` exits 0 (no errors; warnings/info acceptable)
    - `flutter test` exits 0 (Phase 1 test suite still green)
    - `lib/app/router.g.dart` still exists and still codegens cleanly (riverpod_generator not broken by analyzer override)
  </acceptance_criteria>
  <done>Build toolchain works end-to-end with both drift_dev and riverpod_generator coexisting. Phase 1 tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: Spike epubx + receive_sharing_intent_plus (go/no-go)</name>
  <files>
    .planning/phases/02-library-epub-import/spike-notes.md
    test/spike/epubx_spike_test.dart
  </files>
  <read_first>
    - .planning/phases/02-library-epub-import/02-RESEARCH.md (Common Pitfalls sections 1 and Assumptions Log A2, A4)
    - .planning/phases/02-library-epub-import/02-CONTEXT.md (specifics block — "epubx Dart 3.11 compatibility must be verified first")
    - CLAUDE.md (epubx staleness risk section)
  </read_first>
  <action>
    This is a time-boxed spike (~30 minutes max). It verifies two assumptions:
    1. `epubx ^4.0.0` imports and parses a real EPUB under Dart 3.11 without analyzer or runtime errors.
    2. `receive_sharing_intent_plus ^1.6.0` resolves on pub.dev and imports without analyzer errors.

    **Part A — epubx spike:**

    Create a minimal valid EPUB fixture at `test/fixtures/spike.epub`. Use the `archive` package (transitively available via drift's SQLite build chain) OR a manually constructed EPUB from public domain sample sources. The simplest path: commit a tiny real EPUB (~50 KB) from Project Gutenberg — e.g., a short story. Place it at `test/fixtures/spike.epub`.

    Create `test/spike/epubx_spike_test.dart` with the following exact contents:

    ```dart
    import 'dart:io';

    import 'package:epubx/epubx.dart';
    import 'package:flutter_test/flutter_test.dart';

    void main() {
      test('epubx parses a minimal real EPUB under Dart 3.11', () async {
        final file = File('test/fixtures/spike.epub');
        expect(file.existsSync(), isTrue, reason: 'spike fixture missing');
        final bytes = await file.readAsBytes();
        final book = await EpubReader.readBook(bytes);

        // Assertions: book object is non-null, title is a String, chapters list exists
        expect(book, isNotNull);
        expect(book.Title, isA<String>());
        expect(book.Chapters, isA<List>());

        // Print observations for spike-notes.md (captured in test output)
        // ignore: avoid_print
        print('SPIKE epubx: title=${book.Title}, author=${book.Author}, '
            'chapters=${book.Chapters?.length}, hasCover=${book.CoverImage != null}');
      });
    }
    ```

    Run the spike test:
    ```
    flutter test test/spike/epubx_spike_test.dart
    ```

    **Part B — receive_sharing_intent_plus import test:**

    Add a second test in the SAME `test/spike/epubx_spike_test.dart` file (below the first):

    ```dart
    test('receive_sharing_intent_plus imports cleanly', () {
      // Import at top of file; if the import line fails to resolve, flutter_test
      // will refuse to compile this file and the failure is visible.
      expect(true, isTrue); // placeholder — import success IS the test
    });
    ```

    And add at the top of the file:
    ```dart
    // ignore: unused_import
    import 'package:receive_sharing_intent_plus/receive_sharing_intent_plus.dart';
    ```

    Rerun `flutter test test/spike/epubx_spike_test.dart`. Exit 0 confirms both assumptions.

    **Part C — Write spike-notes.md:**

    Create `.planning/phases/02-library-epub-import/spike-notes.md` with EXACT sections:

    ```markdown
    # Phase 2 Spike Results

    **Date:** <today>
    **Ran by:** execute-plan Task 3 of 02-01-PLAN.md

    ## epubx ^4.0.0 Dart 3.11 compatibility — VERDICT: <PASS|FAIL>

    - Fixture: test/fixtures/spike.epub (<bytes> bytes, <source>)
    - EpubReader.readBook returned: <title>, <author or "(null)">, <N> chapters, hasCover=<bool>
    - Analyzer: <clean|errors listed below>
    - Runtime: <no exceptions|exceptions listed below>

    **Decision:** <proceed with epubx | fall back to archive + xml + html>

    ## receive_sharing_intent_plus ^1.6.0 availability — VERDICT: <PASS|FAIL>

    - Pub.dev resolution: <success|failure>
    - Dart import: <clean|errors>
    - Analyzer: <clean|errors>

    **Decision:** <proceed with receive_sharing_intent_plus | fall back to <choice>>

    ## Fallback plans (if any verdict = FAIL)

    - epubx FAIL → implement ~200 LOC custom parser using package:archive + package:xml (for content.opf) + package:html (for chapter XHTML). Estimated +3-4 hours to Plan 04.
    - receive_sharing_intent_plus FAIL → evaluate `app_links` package (URL-intent handler) or write a MethodChannel wrapper in Plan 05. Estimated +1-2 hours to Plan 05.
    ```

    Fill in the EXACT observed values — do not write "TBD" or "TODO". If either PASS, the fallback plan section can be removed.

    If epubx FAILS: STOP. Do not proceed to Plan 02/04 until Jake decides between "fall back to custom parser in Plan 04" vs "revise Plan 04 scope." Surface the failure in the final task summary.
  </action>
  <verify>
    <automated>flutter test test/spike/epubx_spike_test.dart &amp;&amp; test -f .planning/phases/02-library-epub-import/spike-notes.md &amp;&amp; test -f test/fixtures/spike.epub &amp;&amp; grep -q "VERDICT:" .planning/phases/02-library-epub-import/spike-notes.md &amp;&amp; ! grep -q "TBD" .planning/phases/02-library-epub-import/spike-notes.md</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/spike.epub` exists and is a valid EPUB (readable by epubx)
    - `test/spike/epubx_spike_test.dart` exists and imports both `package:epubx/epubx.dart` and `package:receive_sharing_intent_plus/receive_sharing_intent_plus.dart`
    - `flutter test test/spike/epubx_spike_test.dart` exits 0 (both tests pass, or documented FAIL with fallback path written)
    - `.planning/phases/02-library-epub-import/spike-notes.md` exists with both VERDICT sections filled in with real observations (no "TBD")
    - If epubx verdict = PASS: no fallback work required; Plan 04 proceeds as researched
    - If epubx verdict = FAIL: spike-notes.md documents the exact failure and Plan 04 scope adjustment must be flagged in final summary
  </acceptance_criteria>
  <done>Both spikes executed, go/no-go verdicts recorded, Plan 02+ are unblocked OR fallback path is explicitly documented in spike-notes.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → project | Third-party package code runs as part of our app — supply-chain trust boundary |
| dependency_overrides → codegen | Overriding an analyzer transitive dependency may silently break codegen output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | pubspec.lock / transitive deps | accept | Dart/Flutter ecosystem is pinned by lockfile; manual review of pubspec.lock diff in git commit. No SCA tooling in v1. |
| T-02-01-02 | Denial of Service | build_runner codegen | mitigate | Task 2 runs `flutter analyze` and `flutter test` as post-codegen smoke checks; any regression blocks the task. |
| T-02-01-03 | Information disclosure | epubx parser | mitigate | Spike uses a public domain fixture; no user data. Production parser threat model is covered in Plan 04. |
</threat_model>

<verification>
- `flutter pub get` resolves cleanly with the analyzer override
- `dart run build_runner build --delete-conflicting-outputs` exits 0
- `flutter analyze` exits 0 (no errors)
- `flutter test` exits 0 (Phase 1 suite green)
- `flutter test test/spike/epubx_spike_test.dart` exits 0 (both spikes pass, OR failure explicitly documented with fallback path)
- spike-notes.md has no "TBD" placeholders
</verification>

<success_criteria>
Phase 2 can proceed to schema + parser work. Specifically:
1. `drift_dev` is installed and functional (Plan 03 can use `drift_dev schema steps`)
2. `file_picker`, `epubx`, `html`, `receive_sharing_intent_plus` are importable (Plans 04-05 can use them)
3. `riverpod_generator` still works (Plan 05 can use `@riverpod` annotations)
4. Both assumption-heavy packages have recorded go/no-go verdicts with empirical evidence
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-epub-import/02-01-SUMMARY.md` documenting:
- Final pubspec.yaml dependency additions and the analyzer override block
- Codegen smoke test results (build_runner exit code, flutter analyze output, flutter test results)
- Spike verdicts for epubx and receive_sharing_intent_plus (PASS/FAIL + observed values)
- If any spike failed: the exact fallback path for downstream plans
</output>
