---
phase: 01-scaffold-compliance-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "pubspec.yaml declares the exact Phase 1 dependency subset (no sherpa_onnx, no just_audio, no audio_service, no epubx, no file_picker)"
    - "pubspec.yaml declares the `fonts:` section for Literata and Merriweather so Plan 05 can drop .ttf files into place and they resolve via `TextStyle(fontFamily: ...)`"
    - "`mise exec -- flutter pub get` resolves cleanly with the pinned versions"
    - "`mise exec -- dart run build_runner build --delete-conflicting-outputs` executes without errors (no generated files yet because Plan 02 adds no annotated sources, but the toolchain must be ready)"
    - "analysis_options.yaml wires flutter_lints + custom_lint (for riverpod_lint) so later plans' code is lint-checked from the first commit"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Full Phase 1 dependency set + fonts manifest"
      contains: "flutter_riverpod: ^3.3.1"
    - path: "analysis_options.yaml"
      provides: "Lint ruleset wired to flutter_lints + custom_lint plugin host"
      contains: "package:flutter_lints/flutter.yaml"
    - path: "pubspec.lock"
      provides: "Reproducible dependency versions"
  key_links:
    - from: "pubspec.yaml"
      to: "pub.dev resolution"
      via: "flutter pub get"
      pattern: "flutter_riverpod|go_router|drift|drift_flutter"
    - from: "analysis_options.yaml"
      to: "custom_lint plugin host"
      via: "analyzer plugin config"
      pattern: "custom_lint"
---

<objective>
Replace the `flutter create`-generated `pubspec.yaml` with the locked Phase 1 dependency subset (NOT the full STACK.md list — Phase 1 does not import sherpa_onnx, just_audio, audio_service, epubx, file_picker, http, or crypto). Wire `analysis_options.yaml` to flutter_lints + custom_lint + riverpod_lint. Declare the fonts manifest in pubspec so Plan 05 can drop .ttf files and they resolve.

Purpose: A single, small, boring plan that every downstream plan depends on. By keeping it tightly scoped, the Wave 3 plans (Android, iOS, Theme, Drift, Crash) can all run in parallel against a stable base.

Output: Final Phase 1 `pubspec.yaml`, `analysis_options.yaml`, `pubspec.lock` committed, first successful `flutter pub get` + `dart run build_runner build` pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md
@CLAUDE.md

<interfaces>
Plan 02 defines no Dart interfaces of its own. It is infrastructure. Downstream interfaces:
- Plan 05 imports from `flutter_riverpod`, `riverpod_annotation`, `shared_preferences`, `package:flutter/material.dart`
- Plan 06 imports from `drift`, `drift_flutter`
- Plan 07 imports from `path_provider`, `package_info_plus`, `dart:io`, `dart:convert`, `dart:async`, `package:flutter/foundation.dart`
- Plan 08 imports from `go_router`, `flutter_riverpod`, `package:flutter/material.dart`
- All code-gen-using plans (05, 06, 08) produce `*.g.dart` files via build_runner
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write Phase 1 pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml (current state after Plan 01 scaffold — `flutter create` default)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Example 1 full pubspec.yaml, §Phase 1 Dependency Subset, §Pitfall 7 drift_flutter version verification, §Pitfall 8 fonts without pubspec declaration, §Assumptions Log A1/A2)
    - .planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md (D-21 Literata + Merriweather, D-22 no google_fonts runtime)
  </read_first>
  <action>
**Step 1: Verify current versions.** Run:

```
mise exec -- dart pub info drift_flutter | head -n 5
mise exec -- dart pub info package_info_plus | head -n 5
```

Record the actual `latest` version for each (likely `drift_flutter ^0.2.x` and `package_info_plus ^8.x` per 01-RESEARCH.md assumptions A1, A2 — but VERIFY). If they differ from the research estimate, use the verified current version and note the actual version in the SUMMARY.

**Step 2: Overwrite `pubspec.yaml`** with the EXACT contents below (substituting verified versions for `drift_flutter` and `package_info_plus` if they differ from the research defaults):

```yaml
name: murmur
description: "Offline EPUB reader with on-device neural TTS."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^3.0.0

  # Routing
  go_router: ^17.2.0

  # Database
  drift: ^2.32.1
  drift_flutter: ^0.2.0  # VERIFY current at plan time — substitute if different

  # Local storage
  shared_preferences: ^2.3.0
  path_provider: ^2.1.5

  # Crash log metadata
  package_info_plus: ^8.0.0  # VERIFY current major at plan time

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  drift_dev: ^2.32.1
  riverpod_generator: ^4.0.3
  custom_lint: ^0.7.0
  riverpod_lint: ^3.0.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/OFL.txt

  fonts:
    - family: Literata
      fonts:
        - asset: assets/fonts/literata/Literata-Regular.ttf
        - asset: assets/fonts/literata/Literata-Bold.ttf
          weight: 700
    - family: Merriweather
      fonts:
        - asset: assets/fonts/merriweather/Merriweather-Regular.ttf
        - asset: assets/fonts/merriweather/Merriweather-Bold.ttf
          weight: 700
```

DO NOT add any of: `sherpa_onnx`, `just_audio`, `audio_service`, `epubx`, `html`, `file_picker`, `http`, `crypto`, `google_fonts`, `device_info_plus`, `flutter_launcher_icons`, `flutter_native_splash`. These belong to Phases 2, 3, 4, 6, or 7 — not Phase 1.

**Step 3: Run `mise exec -- flutter pub get`.** If pub resolution fails, capture the full error and fix the offending version constraint. Common fixes: if `drift_flutter` 0.2.0 doesn't exist, use the actual current version from Step 1's `dart pub info`.

**Note on fonts section:** The `fonts:` block declares four .ttf files that don't exist yet. `flutter pub get` will NOT fail on missing font files — it only validates `pubspec.yaml` syntax and pub dependencies. The actual font files will be placed by Plan 05. `flutter analyze` and `flutter build` would fail without the files, but Wave 3 lands both Plan 05 and this simultaneously, so it resolves before any build runs.
  </action>
  <verify>
    <automated>grep -q 'flutter_riverpod: \^3.3.1' pubspec.yaml && grep -q 'go_router: \^17.2.0' pubspec.yaml && grep -q 'drift: \^2.32.1' pubspec.yaml && grep -q 'drift_flutter:' pubspec.yaml && grep -q 'shared_preferences:' pubspec.yaml && grep -q 'path_provider:' pubspec.yaml && grep -q 'package_info_plus:' pubspec.yaml && grep -q 'riverpod_generator: \^4.0.3' pubspec.yaml && grep -q 'custom_lint:' pubspec.yaml && grep -q 'riverpod_lint:' pubspec.yaml && grep -q 'flutter_lints:' pubspec.yaml && grep -q 'drift_dev:' pubspec.yaml && grep -q 'family: Literata' pubspec.yaml && grep -q 'family: Merriweather' pubspec.yaml && ! grep -q 'sherpa_onnx' pubspec.yaml && ! grep -q 'just_audio' pubspec.yaml && ! grep -q 'audio_service' pubspec.yaml && ! grep -q 'epubx' pubspec.yaml && ! grep -q 'file_picker' pubspec.yaml && ! grep -q 'google_fonts' pubspec.yaml && mise exec -- flutter pub get</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` contains `flutter_riverpod: ^3.3.1`
    - `pubspec.yaml` contains `riverpod_annotation: ^3.0.0`
    - `pubspec.yaml` contains `go_router: ^17.2.0`
    - `pubspec.yaml` contains `drift: ^2.32.1`
    - `pubspec.yaml` contains `drift_flutter:` (any version)
    - `pubspec.yaml` contains `shared_preferences: ^2.3.0`
    - `pubspec.yaml` contains `path_provider: ^2.1.5`
    - `pubspec.yaml` contains `package_info_plus:`
    - `pubspec.yaml` contains `build_runner: ^2.4.13`
    - `pubspec.yaml` contains `drift_dev: ^2.32.1`
    - `pubspec.yaml` contains `riverpod_generator: ^4.0.3`
    - `pubspec.yaml` contains `custom_lint: ^0.7.0`
    - `pubspec.yaml` contains `riverpod_lint: ^3.0.0`
    - `pubspec.yaml` contains `flutter_lints: ^5.0.0`
    - `pubspec.yaml` contains `family: Literata`
    - `pubspec.yaml` contains `family: Merriweather`
    - `pubspec.yaml` contains `assets/fonts/literata/Literata-Regular.ttf`
    - `pubspec.yaml` contains `assets/fonts/merriweather/Merriweather-Bold.ttf`
    - `pubspec.yaml` does NOT contain `sherpa_onnx`, `just_audio`, `audio_service`, `epubx`, `file_picker`, `google_fonts` (each grep returns empty)
    - `mise exec -- flutter pub get` exits 0
    - `pubspec.lock` exists after pub get (generated)
  </acceptance_criteria>
  <done>pubspec.yaml has the full Phase 1 dependency set + fonts manifest and pub resolves cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire analysis_options.yaml + run first build_runner</name>
  <files>analysis_options.yaml</files>
  <read_first>
    - analysis_options.yaml (current `flutter create`-generated state)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Dev Dependencies, §Pattern 3 re riverpod_lint, §Pitfall 4 re build_runner)
    - CLAUDE.md (Riverpod 3 + codegen convention)
  </read_first>
  <action>
Overwrite `analysis_options.yaml` with the EXACT contents below:

```yaml
# analysis_options.yaml — Phase 1 lint ruleset
# flutter_lints is the official Flutter baseline.
# custom_lint hosts riverpod_lint which catches @riverpod provider mistakes at analysis time.

include: package:flutter_lints/flutter.yaml

analyzer:
  plugins:
    - custom_lint

  errors:
    # Treat TODO comments as info, not warning — Phase 1 has a few.
    todo: info

linter:
  rules:
    # Safer async patterns
    avoid_print: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_locals: true
    require_trailing_commas: true
    use_key_in_widget_constructors: true
```

Then run `mise exec -- dart run build_runner build --delete-conflicting-outputs`. Because Plan 02 adds no `@riverpod` or `@DriftDatabase` annotated sources, build_runner will exit 0 with no generated files — but this proves the codegen pipeline is wired and ready for Plans 05, 06, 08.

Also run `mise exec -- flutter analyze`. With Plan 02's stub `lib/main.dart` this should pass cleanly (the stub is a single `runApp(MaterialApp(...))` call). If it reports lint warnings about `require_trailing_commas` on the stub, fix the stub to satisfy the linter.
  </action>
  <verify>
    <automated>grep -q 'package:flutter_lints/flutter.yaml' analysis_options.yaml && grep -q 'custom_lint' analysis_options.yaml && grep -q 'prefer_const_constructors: true' analysis_options.yaml && mise exec -- dart run build_runner build --delete-conflicting-outputs && mise exec -- flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `analysis_options.yaml` contains `include: package:flutter_lints/flutter.yaml`
    - `analysis_options.yaml` contains `custom_lint` under `analyzer: plugins:`
    - `analysis_options.yaml` contains `prefer_const_constructors: true`
    - `analysis_options.yaml` contains `require_trailing_commas: true`
    - `mise exec -- dart run build_runner build --delete-conflicting-outputs` exits 0
    - `mise exec -- flutter analyze` exits 0 (zero analyzer errors, zero warnings on the stub main.dart)
  </acceptance_criteria>
  <done>analysis_options is wired, build_runner is functional, analyze is green on the stub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev fetch | `flutter pub get` downloads pub packages over HTTPS from pub.dev (one-time per dep bump) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T (Tampering / supply chain) | pub.dev dependency resolution | mitigate | `pubspec.lock` is committed (Plan 01 .gitignore edit leaves it tracked); exact versions pinned for riverpod_generator (`^4.0.3`), build_runner (`^2.4.13`), custom_lint (`^0.7.0`), riverpod_lint (`^3.0.0`); no `any` constraints |
| T-01-03b | T (over-broad deps) | Phase 1 pubspec | mitigate | Phase 1 deliberately excludes sherpa_onnx / just_audio / audio_service / epubx / file_picker — Task 1 `grep -v` checks enforce this |
</threat_model>

<verification>
- `mise exec -- flutter pub get` succeeds
- `mise exec -- dart run build_runner build --delete-conflicting-outputs` succeeds (no sources to generate yet, but pipeline is ready)
- `mise exec -- flutter analyze` succeeds
- `pubspec.lock` is created and contains all expected transitive deps
</verification>

<success_criteria>
Every downstream plan (03-09) can safely assume:
- All Phase 1 pub dependencies are installed
- build_runner is wired and will produce `*.g.dart` files when annotated sources exist
- flutter_lints + riverpod_lint rules are active
- pubspec fonts manifest is ready for Plan 05 to drop .ttf files
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-compliance-foundation/01-02-SUMMARY.md` with: actual resolved `drift_flutter` and `package_info_plus` versions, any pub resolution errors encountered and their fixes, full `pubspec.lock` size / dep count summary.
</output>
