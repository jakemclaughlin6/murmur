---
phase: 01-scaffold-compliance-foundation
plan: 09
type: execute
wave: 5
depends_on: ["01-03", "01-04", "01-08"]
files_modified:
  - .github/workflows/ci.yml
  - README.md
autonomous: true
requirements: [FND-09]
must_haves:
  truths:
    - "`.github/workflows/ci.yml` has an `android` job that runs on `ubuntu-latest`, triggers on `push` to main and pull_request, installs Flutter 3.41.0 via `subosito/flutter-action@v2`, runs `flutter analyze`, `flutter test`, `flutter build appbundle --debug`, and uploads `murmur-debug.aab` as an artifact"
    - "`.github/workflows/ci.yml` has an `ios-scaffold` job that runs on `macos-14`, triggers ONLY on `workflow_dispatch` (guarded by `if: github.event_name == 'workflow_dispatch'`), runs `pod install`, `flutter build ios --no-codesign`, and `xcodebuild archive` with CODE_SIGN_IDENTITY=\"\", uploads `murmur-unsigned.xcarchive`"
    - "The `android` job also runs `bash scripts/verify_android_manifest.sh` as a build gate"
    - "The `ios-scaffold` job runs `bash scripts/verify_ios_plist.sh` as a build gate"
    - "`README.md` has a `## Physical Device Install (Android)` section documenting the bundletool commands to install the CI-built AAB on Jake's Android phone"
    - "`README.md` has a `## Project Structure` section describing the Phase 1 layout"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI with Android (push) and iOS-scaffold (workflow_dispatch) jobs"
      contains: "murmur-debug.aab"
    - path: "README.md"
      provides: "Updated README with physical device install + project structure sections"
      contains: "bundletool"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "android/keys/debug.keystore (committed by Plan 03)"
      via: "flutter build appbundle --debug signingConfig lookup"
      pattern: "flutter build appbundle --debug"
    - from: ".github/workflows/ci.yml"
      to: "scripts/verify_android_manifest.sh + scripts/verify_ios_plist.sh"
      via: "build-gate shell steps"
      pattern: "verify_android_manifest\\.sh|verify_ios_plist\\.sh"
---

<objective>
Ship the GitHub Actions workflow that satisfies FND-09 (amended per D-06): a signed debug Android AAB on every push to main, plus a `workflow_dispatch`-only iOS scaffold job that produces an unsigned `.xcarchive` on `macos-14`. Integrate the verification scripts from Plans 03 and 04 as CI build gates. Complete the README's placeholder sections so Jake has a clear physical-device install path for the CI-produced AAB.

Purpose: CI is the Phase 1 deliverable that proves the entire scaffold is reproducible on a clean machine. Plans 01-08 work on Jake's machine because his machine has mise + Flutter + Android SDK set up. This plan proves the same set of files work on a fresh `ubuntu-latest` runner that only has `actions/setup-java@v4` and `subosito/flutter-action@v2`. If CI goes green, Phase 1 is done.

Output: One workflow file + README updates. No code, no tests beyond what's already in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write .github/workflows/ci.yml with android + ios-scaffold jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - scripts/verify_android_manifest.sh (Plan 03 output)
    - scripts/verify_ios_plist.sh (Plan 04 output)
    - android/app/build.gradle.kts (Plan 03 — confirm signing config wires the committed keystore)
    - .planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md (D-06 amended FND-09)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Example 8 full ci.yml with both jobs)
  </read_first>
  <action>
Create `.github/workflows/ci.yml` with EXACTLY the following content:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  android:
    name: Android (signed debug AAB)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.0'
          channel: 'stable'
          cache: true

      - name: Flutter pub get
        run: flutter pub get

      - name: Generate code (build_runner)
        # *.g.dart files are committed per 01-RESEARCH.md §Pitfall 4 Option A,
        # so this step is technically redundant — but running it guarantees CI
        # and local produce identical generated output. If the committed files
        # are stale, this will fail and force a commit.
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Verify Android manifest allow-list
        run: bash scripts/verify_android_manifest.sh

      - name: Flutter analyze
        run: flutter analyze

      - name: Flutter test
        run: flutter test

      - name: Build signed debug AAB
        # --debug uses the committed debug.keystore via signingConfig "debugCommitted"
        # declared in android/app/build.gradle.kts (Plan 03 Task 2).
        run: flutter build appbundle --debug

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: murmur-debug.aab
          path: build/app/outputs/bundle/debug/app-debug.aab
          if-no-files-found: error
          retention-days: 14

  ios-scaffold:
    name: iOS (workflow_dispatch only — unsigned xcarchive)
    # iOS builds ONLY on manual trigger. Push-to-main does NOT run this job.
    # Per D-06: Phase 1's iOS deliverable is an unsigned .xcarchive proving
    # Info.plist keys + deployment target compile on macos-14. Full signed IPA
    # is deferred to Phase 4 once Apple Developer Program enrollment lands.
    if: github.event_name == 'workflow_dispatch'
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.0'
          channel: 'stable'
          cache: true

      - name: Flutter pub get
        run: flutter pub get

      - name: Generate code (build_runner)
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Verify iOS Info.plist keys
        run: bash scripts/verify_ios_plist.sh

      - name: Flutter analyze
        run: flutter analyze

      - name: Flutter test
        run: flutter test

      - name: Pod install
        run: cd ios && pod install

      - name: Build iOS (unsigned)
        run: flutter build ios --no-codesign --release

      - name: Archive unsigned xcarchive
        run: |
          xcodebuild \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath build/ios/Runner.xcarchive \
            archive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Upload xcarchive artifact
        uses: actions/upload-artifact@v4
        with:
          name: murmur-unsigned.xcarchive
          path: build/ios/Runner.xcarchive
          retention-days: 14
```

**Notes:**
- The `if: github.event_name == 'workflow_dispatch'` guard on `ios-scaffold` means push-to-main and PR checks will NOT run the iOS job — saving macOS runner minutes (~$0.08/min on private repos). Only manual triggers via the "Run workflow" button in the GitHub Actions UI trigger it.
- The `android` job runs on every push AND every PR — matching "on every push to main" from FND-09.
- `actions/checkout@v4`, `actions/setup-java@v4`, `subosito/flutter-action@v2`, `actions/upload-artifact@v4` are the current (2026) versions per 01-RESEARCH.md §Example 8. If GitHub deprecates any of these, the CI job will surface a warning — bump at that point.
- `flutter pub get` and `dart run build_runner build` are repeated in both jobs; that's intentional — each job has its own clean runner.
- If `pod install` fails in CI, it's usually because the committed `ios/Podfile.lock` is out of sync. The fix is to run `cd ios && pod install` locally on a macOS machine and commit the updated `Podfile.lock`. In Phase 1 without a Mac, this is not solvable locally — defer fixing until Phase 4's Mac-access decision. Until then, the `ios-scaffold` job may legitimately fail until someone runs `pod install` on a Mac.
- There is no `push_to_branch` trigger for `ios-scaffold` — per D-06, iOS CI is `workflow_dispatch`-ONLY.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'name: CI' .github/workflows/ci.yml && grep -q 'runs-on: ubuntu-latest' .github/workflows/ci.yml && grep -q 'runs-on: macos-14' .github/workflows/ci.yml && grep -q "flutter-version: '3.41.0'" .github/workflows/ci.yml && grep -q 'flutter build appbundle --debug' .github/workflows/ci.yml && grep -q 'flutter build ios --no-codesign' .github/workflows/ci.yml && grep -q 'murmur-debug.aab' .github/workflows/ci.yml && grep -q 'murmur-unsigned.xcarchive' .github/workflows/ci.yml && grep -q "if: github.event_name == 'workflow_dispatch'" .github/workflows/ci.yml && grep -q 'bash scripts/verify_android_manifest.sh' .github/workflows/ci.yml && grep -q 'bash scripts/verify_ios_plist.sh' .github/workflows/ci.yml && grep -q 'actions/upload-artifact@v4' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - `.github/workflows/ci.yml` contains `name: CI`
    - `.github/workflows/ci.yml` contains `runs-on: ubuntu-latest` (for the android job)
    - `.github/workflows/ci.yml` contains `runs-on: macos-14` (for the ios-scaffold job)
    - `.github/workflows/ci.yml` contains `flutter-version: '3.41.0'`
    - `.github/workflows/ci.yml` contains `flutter build appbundle --debug`
    - `.github/workflows/ci.yml` contains `flutter build ios --no-codesign`
    - `.github/workflows/ci.yml` contains the artifact name `murmur-debug.aab`
    - `.github/workflows/ci.yml` contains the artifact name `murmur-unsigned.xcarchive`
    - `.github/workflows/ci.yml` contains `if: github.event_name == 'workflow_dispatch'` (iOS guard)
    - `.github/workflows/ci.yml` contains `bash scripts/verify_android_manifest.sh`
    - `.github/workflows/ci.yml` contains `bash scripts/verify_ios_plist.sh`
    - `.github/workflows/ci.yml` contains `actions/upload-artifact@v4`
    - `.github/workflows/ci.yml` contains `CODE_SIGN_IDENTITY=""` (unsigned xcarchive)
    - `.github/workflows/ci.yml` triggers on `push` to `main`, `pull_request` to `main`, and `workflow_dispatch`
    - yamllint (if available) exits 0: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`
  </acceptance_criteria>
  <done>CI workflow is complete with both jobs and the correct triggers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill in README.md Physical Device Install + Project Structure sections</name>
  <files>README.md</files>
  <read_first>
    - README.md (Plan 01 stub — has mise bootstrap + placeholder sections for Plan 09 to fill)
    - lib/ directory tree from Plan 08 (to document the project structure accurately)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Physical Device Install Path — bundletool commands)
  </read_first>
  <action>
Overwrite `README.md` with EXACTLY the following content (this replaces Plan 01's stub with the full Phase 1 README):

```markdown
# murmur

Offline EPUB reader with on-device neural TTS. Paid one-time purchase. No accounts, no cloud, no telemetry.

## Bootstrap (first-time setup)

murmur uses [mise](https://mise.jdx.dev/) to pin its toolchain. Nothing is installed to the host system.

```sh
# Install mise (one time)
curl https://mise.run | sh

# Install mise plugins (one time)
mise plugin install flutter https://github.com/mise-plugins/mise-flutter.git
mise plugin install android-sdk https://github.com/mise-plugins/mise-android-sdk.git

# Install the pinned toolchain (Flutter 3.41.0, Java 17, android-sdk)
mise install

# Accept Android SDK licenses + install platform-tools + build-tools + platforms;android-34
mise run setup-android

# Verify toolchain is green
mise exec -- flutter doctor
```

## Running locally

```sh
# Resolve dependencies
mise exec -- flutter pub get

# Run generated code (riverpod + drift)
mise exec -- dart run build_runner build --delete-conflicting-outputs

# Run on a connected Android device
mise exec -- flutter run

# Run tests
mise exec -- flutter test

# Build a signed debug AAB (uses committed debug keystore — DEBUG ONLY, rotated in Phase 7)
mise exec -- flutter build appbundle --debug
```

## Physical Device Install (Android)

The signed debug AAB produced by `flutter build appbundle --debug` is NOT directly installable via `adb install` — AAB files require conversion to APK first. Use Google's `bundletool`:

```sh
# Download bundletool (one time)
curl -L https://github.com/google/bundletool/releases/latest/download/bundletool-all.jar -o bundletool.jar

# Build a universal APK from the signed debug AAB
java -jar bundletool.jar build-apks \
  --bundle=build/app/outputs/bundle/debug/app-debug.aab \
  --output=build/app/outputs/bundle/debug/app.apks \
  --mode=universal \
  --ks=android/keys/debug.keystore \
  --ks-pass=pass:murmurdebug \
  --ks-key-alias=murmurdebug \
  --key-pass=pass:murmurdebug

# Install on a connected device
java -jar bundletool.jar install-apks \
  --apks=build/app/outputs/bundle/debug/app.apks
```

**Simpler path for most dev loops:** use `mise exec -- flutter run --debug` — it installs via the APK pipeline directly and skips the AAB→APK conversion.

The CI-produced `murmur-debug.aab` artifact from GitHub Actions (see [CI section](#continuous-integration)) uses the same committed debug keystore, so it installs with the same bundletool commands above.

## iOS (Phase 1 status)

Phase 1 does **not** produce an installable iOS build. Per the [Phase 1 CONTEXT decisions D-05/D-06](.planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md), Apple Developer Program enrollment is deferred to Phase 4. The Phase 1 iOS deliverable is an unsigned `.xcarchive` produced by the `ios-scaffold` GitHub Actions job, triggered manually via `workflow_dispatch`. The unsigned archive is not installable on a physical iPhone — it only proves that the Info.plist keys, bundle identifier, and iOS 17.0 deployment target compile on `macos-14`.

Full iOS signing, TestFlight uploads, and physical-device installs land in Phase 4 once the Apple Developer Program decision is made.

## Continuous Integration

GitHub Actions workflow: `.github/workflows/ci.yml`

- **`android` job:** runs on every push to main and every PR. Uses `ubuntu-latest`, installs Flutter 3.41.0 via `subosito/flutter-action@v2`, runs analyze + test, builds a signed debug AAB, uploads `murmur-debug.aab` as a workflow artifact (14-day retention).
- **`ios-scaffold` job:** runs ONLY on manual `workflow_dispatch` trigger (not on push or PR). Uses `macos-14`, runs `flutter build ios --no-codesign` and `xcodebuild archive` with `CODE_SIGN_IDENTITY=""`, uploads `murmur-unsigned.xcarchive` as a workflow artifact.

## Project Structure

```
murmur/
├── .github/workflows/ci.yml        # Android (push) + iOS scaffold (workflow_dispatch)
├── .mise.toml                      # Flutter 3.41.0, Java 17, android-sdk
├── android/
│   ├── app/
│   │   ├── build.gradle.kts       # minSdk=24, targetSdk=34, signed debug
│   │   └── src/main/AndroidManifest.xml  # FOREGROUND_SERVICE_MEDIA_PLAYBACK + POST_NOTIFICATIONS
│   └── keys/
│       ├── debug.keystore         # committed — DEBUG ONLY, rotated in Phase 7
│       └── README.md              # debug keystore warning + Phase 7 rotation note
├── ios/
│   ├── Runner/Info.plist          # FND-07 compliance keys (background audio, EPUB UTI, export compliance)
│   ├── Runner.xcodeproj/project.pbxproj  # IPHONEOS_DEPLOYMENT_TARGET = 17.0
│   └── Podfile                    # platform :ios, '17.0' + post_install safety net
├── lib/
│   ├── main.dart                  # triple-catch + ProviderScope + CrashLogger.initialize
│   ├── app/
│   │   ├── app.dart               # MurmurApp — MaterialApp.router consuming theme provider
│   │   └── router.dart            # go_router StatefulShellRoute.indexedStack, 3 branches
│   ├── core/
│   │   ├── crash/                 # JSONL crash logger + 1MB rotation + triple-catch
│   │   ├── db/                    # Drift v1, schemaVersion=1, zero tables
│   │   └── theme/                 # 4 ThemeData builders (Clay neutrals), theme mode provider
│   └── features/
│       ├── library/               # empty-state placeholder + Import CTA (no-op)
│       ├── reader/                # single RichText paragraph in Literata
│       └── settings/              # theme picker + font preview + crash log status
├── assets/fonts/
│   ├── literata/                  # Literata Regular + Bold (OFL)
│   ├── merriweather/              # Merriweather Regular + Bold (OFL)
│   └── OFL.txt
├── drift_schemas/
│   └── drift_schema_v1.json       # Phase 2 migration baseline
├── scripts/
│   ├── verify_android_manifest.sh # CI build gate
│   └── verify_ios_plist.sh        # CI build gate
├── test/
│   ├── crash/                     # CrashLogger unit tests
│   ├── db/                        # AppDatabase unit tests
│   ├── fonts/                     # font_bundle_test
│   ├── theme/                     # 4 themes + persistence tests
│   └── widget/                    # navigation + provider scope widget tests
└── pubspec.yaml
```

## Privacy

murmur makes exactly one network call in its entire lifetime: the one-time Kokoro TTS model download on first launch (Phase 4, not yet implemented). After that, airplane mode is a supported operating state. There are no accounts, no telemetry, no analytics, no crash reporting over the network. Crash logs are written to a local file at `${appDocumentsDir}/crashes/crashes.log` and never leave the device.

## License

Application code: TBD (will be set in Phase 7 before store submission).

Bundled fonts (Literata, Merriweather): [SIL Open Font License 1.1](assets/fonts/OFL.txt).
```
  </action>
  <verify>
    <automated>grep -q '## Physical Device Install (Android)' README.md && grep -q 'bundletool' README.md && grep -q '## Project Structure' README.md && grep -q 'mise install' README.md && grep -q 'StatefulShellRoute' README.md && grep -q 'drift_schema_v1.json' README.md && grep -q 'Phase 4' README.md && grep -q 'Literata' README.md && grep -q 'Merriweather' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Physical Device Install (Android)` (exact section header)
    - `README.md` contains `bundletool`
    - `README.md` contains the 4 bundletool command examples (build-apks, install-apks, etc)
    - `README.md` contains `## Project Structure`
    - `README.md` contains `mise install` (bootstrap path preserved from Plan 01)
    - `README.md` describes the iOS Phase 1 limitation (no signed build, D-05/D-06 reference)
    - `README.md` contains `## Continuous Integration` section
    - `README.md` documents both CI jobs (android + ios-scaffold)
    - `README.md` contains a project structure tree with lib/, test/, assets/fonts/, drift_schemas/, scripts/, .github/workflows/, android/keys/, ios/Runner/
  </acceptance_criteria>
  <done>README is complete with every Phase 1 section Jake needs to hand off the project, install the CI artifact on a physical Android device, and understand what iOS deliverable Phase 1 produced (and what it did not).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions workflow | CI runners are public fork-accessible on push/PR; artifacts are workflow-scoped by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | I (Info disclosure) | CI artifact cross-fork access | mitigate | `actions/upload-artifact@v4` scopes artifacts to the workflow run ID. PRs from forks cannot download artifacts from base-repo workflow runs by default (GitHub's permission model). The committed debug.keystore is already public in the repo, so it's not leaked by CI — its DEBUG-ONLY status is documented in `android/keys/README.md` |
| T-01-01 | S (Spoofing via Play Store) | Debug keystore reused in Phase 7 release | mitigate | The `release` buildType in build.gradle.kts uses the debug keystore for Phase 1 — this is documented as a Phase 7 rotation item. Phase 9's CI workflow has NO release upload path, so there's no way to accidentally push this to the Play Store. The first-ever Play Store upload happens in Phase 7 with a freshly rotated upload keystore |
| T-01-V10 | V10 (supply chain) | subosito/flutter-action@v2 and actions/upload-artifact@v4 pinned | accept | GitHub action pins to major versions (v2, v4) — acceptable trust boundary for a solo-dev project. If supply-chain sensitivity increases, pin to SHA in a future phase |
</threat_model>

<verification>
After merging Plan 09:
- `git push` to main triggers the `android` CI job
- The `android` job runs `flutter analyze`, `flutter test`, `flutter build appbundle --debug`, and uploads the AAB
- `bash scripts/verify_android_manifest.sh` passes as a CI step
- Manual trigger of the `ios-scaffold` workflow (via GitHub Actions UI "Run workflow" button) produces an unsigned xcarchive artifact
- Jake downloads `murmur-debug.aab` from the GitHub Actions run, runs the bundletool commands from README.md, and installs the app on a physical Android phone
</verification>

<success_criteria>
FND-09 (amended per D-06) is satisfied:
- On every push to main, CI produces a signed debug Android AAB downloadable as an artifact
- On manual `workflow_dispatch` trigger, CI produces an unsigned `.xcarchive` for iOS

Phase 1 is COMPLETE when:
- All 9 plans' automated verifications are green
- CI green on main for at least one push
- Jake can follow README.md's Physical Device Install section and launch the app on his Android phone
- `01-VALIDATION.md` has been updated with real plan+task IDs replacing `01-XX-XX` placeholders, and `nyquist_compliant: true` is set in its frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-compliance-foundation/01-09-SUMMARY.md` with: first CI run URL + result, full output of `bash scripts/verify_android_manifest.sh` and `bash scripts/verify_ios_plist.sh` from the CI logs, any `pod install` errors encountered on the first `ios-scaffold` run (likely — Phase 1 without Mac access cannot generate a working Podfile.lock locally), any README corrections.
</output>
