---
phase: 01-scaffold-compliance-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - android/app/build.gradle.kts
  - android/keys/debug.keystore
  - android/keys/README.md
  - android/app/src/main/AndroidManifest.xml
  - scripts/verify_android_manifest.sh
autonomous: true
requirements: [FND-01, FND-08]
must_haves:
  truths:
    - "`applicationId = dev.jmclaughlin.murmur` is set in `android/app/build.gradle.kts`"
    - "Android `minSdkVersion = 24`, `targetSdkVersion = 34`, `compileSdkVersion = 34` (per D-24, D-25)"
    - "A deterministic debug keystore is committed at `android/keys/debug.keystore` with password `murmurdebug` (per D-04), wired as the signingConfig for both debug and release buildTypes"
    - "AndroidManifest.xml declares exactly `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MEDIA_PLAYBACK`, and `POST_NOTIFICATIONS` permissions — and does NOT declare `READ_MEDIA_AUDIO` (per 01-RESEARCH.md §AndroidManifest critical note)"
    - "AndroidManifest.xml android:label is `Murmur` (per D-02 display name)"
    - "`scripts/verify_android_manifest.sh` exits 0 when all required permissions are present and declared permissions are within the allowed set"
    - "`mise exec -- flutter build appbundle --debug` produces a signed AAB (Plan 09 runs this in CI — this plan proves it builds locally)"
  artifacts:
    - path: "android/app/build.gradle.kts"
      provides: "Android build config — applicationId, SDK floors, signing config"
      contains: "dev.jmclaughlin.murmur"
    - path: "android/keys/debug.keystore"
      provides: "Committed deterministic debug keystore for signed debug AAB"
    - path: "android/keys/README.md"
      provides: "DEBUG-ONLY warning + Phase 7 rotation pointer"
      contains: "DEBUG-ONLY"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Manifest with Phase 4-ready TTS foreground permissions"
      contains: "FOREGROUND_SERVICE_MEDIA_PLAYBACK"
    - path: "scripts/verify_android_manifest.sh"
      provides: "Shell assertion that manifest permissions match the allow-list exactly"
  key_links:
    - from: "android/app/build.gradle.kts"
      to: "android/keys/debug.keystore"
      via: "signingConfigs.debugCommitted.storeFile = file(\"../keys/debug.keystore\")"
      pattern: "storeFile = file\\(\"\\.\\./keys/debug\\.keystore\"\\)"
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android:label=\"Murmur\""
      via: "application label"
      pattern: "android:label=\"Murmur\""
---

<objective>
Configure Android for FND-01 (correct bundle ID, display name, SDK floors) and FND-08 (foreground service + notifications permissions for future TTS background audio). Commit a deterministic debug keystore (D-04) so CI can produce a "signed debug AAB" without any secrets plumbing. Write a verification script that shell-asserts the manifest matches the exact allow-list (no over-ask).

Purpose: Phase 1 is about compliance groundwork. The manifest permissions and the signing config must be correct NOW because Plan 09's CI job runs `flutter build appbundle --debug` against whatever this plan produces. If the keystore or SDK floors are wrong, CI fails at green-or-red — no intermediate states.

Output: A build-ready Android configuration that produces a signed debug AAB locally via `mise exec -- flutter build appbundle --debug`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate + commit debug keystore, write android/keys/README.md</name>
  <files>android/keys/debug.keystore, android/keys/README.md</files>
  <read_first>
    - .planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md (D-03 Jake McLaughlin as publisher, D-04 committed debug keystore)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Pitfall 3 reproducible keystore, §Example 3 keytool command)
    - .gitignore (confirm Plan 01 removed any `*.keystore` or `**/keystore/*` entries)
  </read_first>
  <action>
Generate the debug keystore with EXACT, reproducible parameters. Run:

```
mkdir -p android/keys
mise exec -- keytool -genkey -v \
  -keystore android/keys/debug.keystore \
  -alias murmurdebug \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -storepass murmurdebug -keypass murmurdebug \
  -dname "CN=Jake McLaughlin, O=Murmur, C=US"
```

(`mise exec --` runs keytool via the mise-installed JDK 17 rather than any host JDK.)

Then create `android/keys/README.md` with EXACTLY the following content:

```markdown
# Debug Keystore — DEBUG-ONLY

This keystore is committed to the repo so CI can produce a "signed debug AAB" without secrets plumbing. It is **DEBUG-ONLY**. Never use it for a Play Store upload.

## Details

- **Path:** `android/keys/debug.keystore`
- **Alias:** `murmurdebug`
- **Password (store and key):** `murmurdebug`
- **DN:** `CN=Jake McLaughlin, O=Murmur, C=US`
- **Validity:** 10000 days
- **Algorithm:** RSA 2048

## Phase 7 Rotation

Before Phase 7 Play Store upload (requirement QAL-05), this keystore MUST be replaced with an upload keystore stored in GitHub Secrets. The `signingConfigs` block in `android/app/build.gradle.kts` will be updated to read `storePassword`, `keyAlias`, and `keyPassword` from environment variables injected by CI. Phase 7 plan should include a "keystore rotation" task.

## Warning

If Play Store ever sees a build signed with this debug keystore, the app identity is **permanently burned** — Google does not allow re-uploads with a different signing cert for the same package name. Double-check every Phase 7 CI job uses the rotated upload keystore before pushing.
```

Verify the keystore exists and is a valid JKS:

```
mise exec -- keytool -list -keystore android/keys/debug.keystore -storepass murmurdebug
```

Should show:
```
Your keystore contains 1 entry
murmurdebug, <date>, PrivateKeyEntry,
Certificate fingerprint (SHA-256): ...
```
  </action>
  <verify>
    <automated>test -f android/keys/debug.keystore && test -s android/keys/debug.keystore && grep -q 'DEBUG-ONLY' android/keys/README.md && grep -q 'Phase 7 Rotation' android/keys/README.md && grep -q 'murmurdebug' android/keys/README.md && mise exec -- keytool -list -keystore android/keys/debug.keystore -storepass murmurdebug | grep -q 'PrivateKeyEntry'</automated>
  </verify>
  <acceptance_criteria>
    - `android/keys/debug.keystore` exists and is non-empty
    - `mise exec -- keytool -list -keystore android/keys/debug.keystore -storepass murmurdebug` shows 1 entry with alias `murmurdebug` and type `PrivateKeyEntry`
    - `android/keys/README.md` contains `DEBUG-ONLY`
    - `android/keys/README.md` contains `Phase 7 Rotation`
    - `android/keys/README.md` contains `murmurdebug` (documents the alias + password)
    - `git status` (or equivalent) shows `android/keys/debug.keystore` as tracked (not gitignored — verify Plan 01 left `.gitignore` clean)
  </acceptance_criteria>
  <done>Committed debug keystore exists with documented alias/password and a Phase 7 rotation warning.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure android/app/build.gradle.kts with SDK floors + signing</name>
  <files>android/app/build.gradle.kts</files>
  <read_first>
    - android/app/build.gradle.kts (current `flutter create`-generated state)
    - .planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md (D-01 bundle ID, D-24 minSdk=24, D-25 targetSdk/compileSdk=34)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Example 3 full build.gradle.kts)
  </read_first>
  <action>
Overwrite `android/app/build.gradle.kts` with EXACTLY the following (note: Flutter 3.41's `flutter create` template may use slightly different structure — preserve any Flutter-template lines not shown below but INSERT/REPLACE the `android { ... }` block with this exact content):

```kotlin
plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin")
}

android {
    namespace = "dev.jmclaughlin.murmur"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17.toString()
    }

    defaultConfig {
        applicationId = "dev.jmclaughlin.murmur"
        minSdk = 24
        targetSdk = 34
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    signingConfigs {
        create("debugCommitted") {
            storeFile = file("../keys/debug.keystore")
            storePassword = "murmurdebug"
            keyAlias = "murmurdebug"
            keyPassword = "murmurdebug"
        }
    }

    buildTypes {
        debug {
            signingConfig = signingConfigs.getByName("debugCommitted")
        }
        release {
            // Phase 1 uses the committed debug keystore for release too so CI can produce
            // a "signed debug AAB" with zero secrets plumbing. Phase 7 (QAL-05) replaces
            // this with an upload keystore from GitHub Secrets. See android/keys/README.md.
            signingConfig = signingConfigs.getByName("debugCommitted")
        }
    }
}

flutter {
    source = "../.."
}
```

Then run a local smoke build to prove everything is wired:

```
mise exec -- flutter build appbundle --debug
```

This MUST produce `build/app/outputs/bundle/debug/app-debug.aab`. If it fails, fix the gradle config before moving on. Common failures:
- `SDK location not found` → Plan 01's mise setup is broken; revisit Task 2 of Plan 01
- `keystore does not exist` → keystore path in `signingConfigs` doesn't match actual file; use exact path `../keys/debug.keystore` (relative to `android/app/`)
- `Java 17 required` → mise Java 17 not in PATH; check `mise exec -- java -version` shows `17.x`
  </action>
  <verify>
    <automated>grep -q 'applicationId = "dev.jmclaughlin.murmur"' android/app/build.gradle.kts && grep -q 'compileSdk = 34' android/app/build.gradle.kts && grep -q 'minSdk = 24' android/app/build.gradle.kts && grep -q 'targetSdk = 34' android/app/build.gradle.kts && grep -q 'namespace = "dev.jmclaughlin.murmur"' android/app/build.gradle.kts && grep -q 'storeFile = file("../keys/debug.keystore")' android/app/build.gradle.kts && grep -q 'keyAlias = "murmurdebug"' android/app/build.gradle.kts && mise exec -- flutter build appbundle --debug && test -f build/app/outputs/bundle/debug/app-debug.aab</automated>
  </verify>
  <acceptance_criteria>
    - `android/app/build.gradle.kts` contains `applicationId = "dev.jmclaughlin.murmur"`
    - `android/app/build.gradle.kts` contains `compileSdk = 34`
    - `android/app/build.gradle.kts` contains `minSdk = 24`
    - `android/app/build.gradle.kts` contains `targetSdk = 34`
    - `android/app/build.gradle.kts` contains `namespace = "dev.jmclaughlin.murmur"`
    - `android/app/build.gradle.kts` contains `storeFile = file("../keys/debug.keystore")`
    - `android/app/build.gradle.kts` contains `storePassword = "murmurdebug"`
    - `android/app/build.gradle.kts` contains `keyAlias = "murmurdebug"`
    - `android/app/build.gradle.kts` contains `sourceCompatibility = JavaVersion.VERSION_17`
    - `mise exec -- flutter build appbundle --debug` exits 0
    - `build/app/outputs/bundle/debug/app-debug.aab` exists after build
  </acceptance_criteria>
  <done>Gradle config compiles, local debug AAB builds successfully, signing config resolves the committed keystore.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write AndroidManifest.xml permissions + label + verification script</name>
  <files>android/app/src/main/AndroidManifest.xml, scripts/verify_android_manifest.sh</files>
  <read_first>
    - android/app/src/main/AndroidManifest.xml (current `flutter create`-generated state)
    - .planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md (D-02 Murmur display name, code_context §AndroidManifest)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Example 4 AndroidManifest.xml with critical READ_MEDIA_AUDIO note, §Pitfall 5 POST_NOTIFICATIONS runtime scope, §Example 4 audio_service stanza deferral advice)
  </read_first>
  <action>
Overwrite `android/app/src/main/AndroidManifest.xml` with EXACTLY the following:

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- FND-08: TTS foreground service permissions (declared now, used in Phase 4).
         Phase 1 declares them but does NOT request runtime permissions or start the
         service — that's Phase 4 scope. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Notification channel for media playback (API 33+). Runtime request happens in
         Phase 4 when audio_service actually posts a lock-screen media notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- INTENTIONALLY NOT DECLARED:
         - READ_MEDIA_AUDIO / READ_EXTERNAL_STORAGE: murmur imports EPUBs via SAF
           (Storage Access Framework) through file_picker's ACTION_OPEN_DOCUMENT,
           not via MediaStore. Declaring READ_MEDIA_AUDIO would be a false permission
           over-request and a Play Store data-access declaration we don't need.
         - INTERNET: Phase 4 will add this for the one-time Kokoro model download.
           Phase 1 has no network calls.
         See 01-RESEARCH.md §Example 4 critical note. -->

    <application
        android:label="Murmur"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- NOTE: audio_service `<service>` stanza is NOT declared in Phase 1. Plan 03
             defers it to Phase 4 where audio_service actually lives in pubspec.yaml.
             Declaring it now with no backing package causes manifest-merger warnings
             for no benefit. See 01-RESEARCH.md §Example 4. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
```

**CRITICAL constraints on this manifest:**
- DO NOT add `android.permission.INTERNET` (Phase 4)
- DO NOT add `android.permission.READ_MEDIA_AUDIO` (never — see note above)
- DO NOT add `android.permission.READ_EXTERNAL_STORAGE` (SAF handles it)
- DO NOT add the `com.ryanheise.audioservice.AudioService` `<service>` stanza (Phase 4)

Now create `scripts/verify_android_manifest.sh` with EXACTLY the following:

```bash
#!/usr/bin/env bash
# Verifies Phase 1 AndroidManifest.xml matches the expected permission allow-list.
# Exit 0 on success, non-zero on any violation.

set -euo pipefail

MANIFEST="android/app/src/main/AndroidManifest.xml"

if [ ! -f "$MANIFEST" ]; then
  echo "FAIL: $MANIFEST does not exist"
  exit 1
fi

fail=0

# Required permissions (must be present)
required=(
  "android.permission.FOREGROUND_SERVICE"
  "android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"
  "android.permission.POST_NOTIFICATIONS"
)

for perm in "${required[@]}"; do
  if ! grep -q "$perm" "$MANIFEST"; then
    echo "FAIL: required permission missing: $perm"
    fail=1
  fi
done

# Forbidden permissions (must NOT be present in Phase 1)
forbidden=(
  "android.permission.INTERNET"
  "android.permission.READ_MEDIA_AUDIO"
  "android.permission.READ_EXTERNAL_STORAGE"
  "android.permission.WRITE_EXTERNAL_STORAGE"
)

for perm in "${forbidden[@]}"; do
  if grep -q "$perm" "$MANIFEST"; then
    echo "FAIL: forbidden permission present: $perm (not allowed in Phase 1)"
    fail=1
  fi
done

# android:label must be Murmur (D-02)
if ! grep -q 'android:label="Murmur"' "$MANIFEST"; then
  echo "FAIL: android:label is not \"Murmur\" (expected per D-02)"
  fail=1
fi

if [ "$fail" -eq 0 ]; then
  echo "OK: AndroidManifest.xml matches Phase 1 allow-list"
  exit 0
else
  exit 1
fi
```

Make it executable: `chmod +x scripts/verify_android_manifest.sh`.

Run it to verify both files are correct:

```
bash scripts/verify_android_manifest.sh
```

Should print `OK: AndroidManifest.xml matches Phase 1 allow-list` and exit 0.

Finally, re-run `mise exec -- flutter build appbundle --debug` to confirm the manifest changes don't break the build (they shouldn't — the manifest only adds permissions and changes the label).
  </action>
  <verify>
    <automated>grep -q 'android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK' android/app/src/main/AndroidManifest.xml && grep -q 'android.permission.POST_NOTIFICATIONS' android/app/src/main/AndroidManifest.xml && grep -q 'android:label="Murmur"' android/app/src/main/AndroidManifest.xml && ! grep -q 'android.permission.INTERNET' android/app/src/main/AndroidManifest.xml && ! grep -q 'android.permission.READ_MEDIA_AUDIO' android/app/src/main/AndroidManifest.xml && ! grep -q 'android.permission.READ_EXTERNAL_STORAGE' android/app/src/main/AndroidManifest.xml && test -x scripts/verify_android_manifest.sh && bash scripts/verify_android_manifest.sh && mise exec -- flutter build appbundle --debug</automated>
  </verify>
  <acceptance_criteria>
    - `android/app/src/main/AndroidManifest.xml` contains `android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK`
    - `android/app/src/main/AndroidManifest.xml` contains `android.permission.FOREGROUND_SERVICE`
    - `android/app/src/main/AndroidManifest.xml` contains `android.permission.POST_NOTIFICATIONS`
    - `android/app/src/main/AndroidManifest.xml` contains `android:label="Murmur"` (exact string with double quotes)
    - `android/app/src/main/AndroidManifest.xml` does NOT contain `android.permission.INTERNET`
    - `android/app/src/main/AndroidManifest.xml` does NOT contain `android.permission.READ_MEDIA_AUDIO`
    - `android/app/src/main/AndroidManifest.xml` does NOT contain `android.permission.READ_EXTERNAL_STORAGE`
    - `android/app/src/main/AndroidManifest.xml` does NOT contain `com.ryanheise.audioservice.AudioService`
    - `scripts/verify_android_manifest.sh` exists and is executable (`test -x`)
    - `bash scripts/verify_android_manifest.sh` exits 0 and prints `OK: AndroidManifest.xml matches Phase 1 allow-list`
    - `mise exec -- flutter build appbundle --debug` still exits 0 after manifest changes
  </acceptance_criteria>
  <done>Manifest has exactly the Phase 1 permissions, verification script is canonical, build still produces a signed debug AAB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android manifest surface | Declared permissions become the app's permission request set — Play Store and user see them |
| Committed keystore | The signing key for the debug AAB lives in the repo (public if repo is public) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S (Spoofing — store impersonation) | `android/keys/debug.keystore` reused in Phase 7 | mitigate | Task 1 creates `android/keys/README.md` with explicit `DEBUG-ONLY` + Phase 7 rotation warning. Phase 7 plan will include an explicit keystore-rotation task. `build.gradle.kts` comment on `release { signingConfig = debugCommitted }` flags Phase 1 exception |
| T-01-04 | I / V10 (least privilege) | AndroidManifest.xml permission over-ask | mitigate | Task 3 `scripts/verify_android_manifest.sh` asserts the forbidden list (INTERNET, READ_MEDIA_AUDIO, READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE) is NOT present AND the required list IS present. Phase 1 declares only what Phase 4 needs |
| T-01-05 | I | Keystore password in plaintext in build.gradle.kts | accept | Keystore is DEBUG-ONLY and `murmurdebug` is documented in README — this is not a security boundary. Phase 7 moves to GitHub Secrets |
</threat_model>

<verification>
- `mise exec -- flutter build appbundle --debug` produces `build/app/outputs/bundle/debug/app-debug.aab`
- `bash scripts/verify_android_manifest.sh` passes
- `keytool -list -keystore android/keys/debug.keystore` shows the `murmurdebug` alias as `PrivateKeyEntry`
- The produced AAB's manifest (unzip and inspect) contains exactly the 3 permissions declared and nothing more
</verification>

<success_criteria>
Plan 09 CI can run `flutter build appbundle --debug` on `ubuntu-latest` and produce a byte-identical signing-stable output. FND-01 (Android side) and FND-08 are both satisfied by the manifest + gradle state this plan produces.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-compliance-foundation/01-03-SUMMARY.md` with: keystore SHA-256 fingerprint from `keytool -list`, exact size of the produced AAB, output of `scripts/verify_android_manifest.sh`, any gradle config adjustments made to accommodate the Flutter 3.41 template (if the default template differs from the research example).
</output>
