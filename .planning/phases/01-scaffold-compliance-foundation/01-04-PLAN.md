---
phase: 01-scaffold-compliance-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - ios/Runner/Info.plist
  - ios/Podfile
  - ios/Runner.xcodeproj/project.pbxproj
  - scripts/verify_ios_plist.sh
autonomous: true
requirements: [FND-01, FND-07]
must_haves:
  truths:
    - "`ios/Runner/Info.plist` declares `CFBundleDisplayName = Murmur`, `UIBackgroundModes = [audio]`, `UIFileSharingEnabled = true`, `LSSupportsOpeningDocumentsInPlace = true`, `CFBundleDocumentTypes` with `org.idpf.epub-container`, and `ITSAppUsesNonExemptEncryption = false`"
    - "`IPHONEOS_DEPLOYMENT_TARGET = 17.0` is set in all three places: `ios/Runner.xcodeproj/project.pbxproj` (Debug/Release/Profile), `ios/Podfile` `platform :ios`, and the Podfile's `post_install` build settings loop"
    - "`scripts/verify_ios_plist.sh` shell-asserts every FND-07 key is present and passes on a Linux host (no `plutil` — use grep/xmllint since Jake has no Mac)"
    - "Bundle identifier `dev.jmclaughlin.murmur` remains set in `project.pbxproj` (not accidentally edited)"
  artifacts:
    - path: "ios/Runner/Info.plist"
      provides: "iOS compliance keys for background audio, file sharing, EPUB UTI, export compliance"
      contains: "UIBackgroundModes"
    - path: "ios/Podfile"
      provides: "iOS 17.0 platform pin with post_install safety net"
      contains: "platform :ios, '17.0'"
    - path: "ios/Runner.xcodeproj/project.pbxproj"
      provides: "Xcode project with IPHONEOS_DEPLOYMENT_TARGET = 17.0"
      contains: "IPHONEOS_DEPLOYMENT_TARGET = 17.0;"
    - path: "scripts/verify_ios_plist.sh"
      provides: "Linux-compatible shell assertion for all FND-07 keys + CFBundleDisplayName"
  key_links:
    - from: "ios/Runner/Info.plist"
      to: "CFBundleDisplayName"
      via: "plist string value"
      pattern: "<key>CFBundleDisplayName</key>\\s*<string>Murmur</string>"
    - from: "ios/Runner/Info.plist"
      to: "org.idpf.epub-container (EPUB UTI)"
      via: "CFBundleDocumentTypes → LSItemContentTypes"
      pattern: "org\\.idpf\\.epub-container"
---

<objective>
Configure iOS for FND-01 (correct bundle ID, display name "Murmur") and FND-07 (all required Info.plist compliance keys for Phase 4 background audio, Phase 2 EPUB file sharing, and the truthful "no non-exempt crypto" export compliance declaration). Set `IPHONEOS_DEPLOYMENT_TARGET = 17.0` in all three places where it lives (pbxproj, Podfile, Podfile post_install) per D-26. Ship a Linux-compatible verification shell script since Jake has no Mac.

Purpose: FND-07 is the "if we don't get this right in Phase 1, Phase 7 App Store submission blocks" compliance gate. Every key declared here unblocks something later — `UIBackgroundModes=audio` unblocks Phase 4 `audio_service`, `LSSupportsOpeningDocumentsInPlace` unblocks Phase 2 share-to-app imports, `ITSAppUsesNonExemptEncryption=false` unblocks the App Store Connect export-compliance dialog. Getting them right once here means Phase 7 upload is purely mechanical.

Output: Fully-edited Info.plist, pbxproj, Podfile, plus a shell script that Plan 09's CI `ios-scaffold` job can call to assert the plist is still correct on `macos-14`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write ios/Runner/Info.plist with all FND-07 keys</name>
  <files>ios/Runner/Info.plist</files>
  <read_first>
    - ios/Runner/Info.plist (current `flutter create`-generated state — preserve any keys not explicitly overridden)
    - .planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md (D-02 Murmur display name, FND-07 full key list)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Example 5 full Info.plist XML, §Pitfall 2 deployment target)
  </read_first>
  <action>
Overwrite `ios/Runner/Info.plist` with EXACTLY the following. This preserves all Flutter-standard keys AND adds every FND-07 compliance key:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Murmur</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>murmur</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>

	<!-- FND-07: Phase 1 compliance keys.
	     These keys unblock Phase 2 (file sharing), Phase 4 (background audio), and
	     Phase 7 (App Store export compliance). Do NOT remove any of them. -->

	<!-- Background audio for Phase 4 TTS playback -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>

	<!-- Files.app integration for Phase 2 EPUB import (share/open-in flow) -->
	<key>UIFileSharingEnabled</key>
	<true/>
	<key>LSSupportsOpeningDocumentsInPlace</key>
	<true/>

	<!-- EPUB document type registration. org.idpf.epub-container is the Apple-registered
	     UTI for EPUB files (used by Files app, Mail attachments, Safari downloads). -->
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>EPUB</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>LSHandlerRank</key>
			<string>Alternate</string>
			<key>LSItemContentTypes</key>
			<array>
				<string>org.idpf.epub-container</string>
			</array>
		</dict>
	</array>

	<!-- Export compliance: murmur uses no non-exempt cryptography.
	     Phase 1 has no network calls. Phase 4 adds a one-time HTTPS model download
	     which is exempt under §740.17(b) of the EAR. This key MUST remain `false`
	     through every Phase; re-verify in Phase 4's transition. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
</dict>
</plist>
```

Do NOT change `CFBundleIdentifier` — it must stay as `$(PRODUCT_BUNDLE_IDENTIFIER)` because that variable is driven by `project.pbxproj`'s `PRODUCT_BUNDLE_IDENTIFIER = dev.jmclaughlin.murmur;` setting. Hand-editing it here would break the variable substitution and allow the pbxproj value to drift undetected.
  </action>
  <verify>
    <automated>grep -q '<string>Murmur</string>' ios/Runner/Info.plist && grep -q '<key>UIBackgroundModes</key>' ios/Runner/Info.plist && grep -q '<key>UIFileSharingEnabled</key>' ios/Runner/Info.plist && grep -q '<key>LSSupportsOpeningDocumentsInPlace</key>' ios/Runner/Info.plist && grep -q 'org.idpf.epub-container' ios/Runner/Info.plist && grep -q '<key>ITSAppUsesNonExemptEncryption</key>' ios/Runner/Info.plist && grep -q 'PRODUCT_BUNDLE_IDENTIFIER' ios/Runner/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - `ios/Runner/Info.plist` contains `<key>CFBundleDisplayName</key>` followed by `<string>Murmur</string>`
    - `ios/Runner/Info.plist` contains `<key>UIBackgroundModes</key>` followed by `<array>` containing `<string>audio</string>`
    - `ios/Runner/Info.plist` contains `<key>UIFileSharingEnabled</key>` followed by `<true/>`
    - `ios/Runner/Info.plist` contains `<key>LSSupportsOpeningDocumentsInPlace</key>` followed by `<true/>`
    - `ios/Runner/Info.plist` contains `<key>CFBundleDocumentTypes</key>`
    - `ios/Runner/Info.plist` contains `org.idpf.epub-container`
    - `ios/Runner/Info.plist` contains `<key>ITSAppUsesNonExemptEncryption</key>` followed by `<false/>`
    - `ios/Runner/Info.plist` contains `PRODUCT_BUNDLE_IDENTIFIER` (variable substitution, not hard-coded bundle ID)
    - `xmllint --noout ios/Runner/Info.plist` exits 0 (valid XML) — skip if xmllint not available
  </acceptance_criteria>
  <done>Info.plist has every FND-07 key and the CFBundleDisplayName set to Murmur. Bundle ID variable substitution is preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Patch IPHONEOS_DEPLOYMENT_TARGET to 17.0 in pbxproj + Podfile</name>
  <files>ios/Runner.xcodeproj/project.pbxproj, ios/Podfile</files>
  <read_first>
    - ios/Runner.xcodeproj/project.pbxproj (current default iOS deployment target — likely 12.0 or 13.0 per 01-RESEARCH.md Assumption A8)
    - ios/Podfile (current `platform :ios, '...'` line)
    - .planning/phases/01-scaffold-compliance-foundation/01-CONTEXT.md (D-26 iOS 17.0)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Pitfall 2 three places, §Example 6 Podfile, §Example 7 sed one-liner)
  </read_first>
  <action>
**Step 1: Record the current default** (for the SUMMARY — this answers research Open Question 4):

```
grep IPHONEOS_DEPLOYMENT_TARGET ios/Runner.xcodeproj/project.pbxproj | head -n 3
grep -E "^\s*#?\s*platform :ios" ios/Podfile
```

**Step 2: Patch `ios/Runner.xcodeproj/project.pbxproj`** with the sed one-liner from 01-RESEARCH.md §Example 7:

```
sed -i "s|IPHONEOS_DEPLOYMENT_TARGET = [0-9.]*;|IPHONEOS_DEPLOYMENT_TARGET = 17.0;|g" ios/Runner.xcodeproj/project.pbxproj
```

Then verify all 3 build configurations are updated:

```
grep IPHONEOS_DEPLOYMENT_TARGET ios/Runner.xcodeproj/project.pbxproj
```

Should print 3 lines, ALL showing `IPHONEOS_DEPLOYMENT_TARGET = 17.0;`.

**Step 3: Replace `ios/Podfile`** — overwrite with EXACTLY the following (based on 01-RESEARCH.md §Example 6):

```ruby
# ios/Podfile — Phase 1: iOS 17.0 platform pin
platform :ios, '17.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end
  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '17.0'
    end
  end
end
```

The top-level `platform :ios, '17.0'` sets the podfile's minimum, and the `post_install` block's `target.build_configurations.each` loop is the safety net that forces every transitive pod to also use iOS 17.0 as its minimum (preventing a single pod from silently dropping the effective target).

**Step 4: Verify bundle ID is untouched** in pbxproj:

```
grep 'PRODUCT_BUNDLE_IDENTIFIER = dev.jmclaughlin.murmur' ios/Runner.xcodeproj/project.pbxproj
```

Must return at least 3 matches (Debug/Release/Profile). If not, the sed patch ACCIDENTALLY clobbered something and the file needs to be restored from git and re-patched with a more specific regex.

Note: there is no `pod install` step in this plan. `pod install` requires CocoaPods (Ruby gem) and realistically only runs on `macos-14` in CI. Plan 09's `ios-scaffold` CI job will run `cd ios && pod install` inside the macOS runner. The Linux host does not need CocoaPods.
  </action>
  <verify>
    <automated>grep -c 'IPHONEOS_DEPLOYMENT_TARGET = 17.0;' ios/Runner.xcodeproj/project.pbxproj | awk '$1 >= 3 {exit 0} {exit 1}' && ! grep -q 'IPHONEOS_DEPLOYMENT_TARGET = 12' ios/Runner.xcodeproj/project.pbxproj && ! grep -q 'IPHONEOS_DEPLOYMENT_TARGET = 13' ios/Runner.xcodeproj/project.pbxproj && ! grep -q 'IPHONEOS_DEPLOYMENT_TARGET = 14' ios/Runner.xcodeproj/project.pbxproj && ! grep -q 'IPHONEOS_DEPLOYMENT_TARGET = 15' ios/Runner.xcodeproj/project.pbxproj && ! grep -q 'IPHONEOS_DEPLOYMENT_TARGET = 16' ios/Runner.xcodeproj/project.pbxproj && grep -q "^platform :ios, '17.0'" ios/Podfile && grep -q "IPHONEOS_DEPLOYMENT_TARGET" ios/Podfile && grep -c 'PRODUCT_BUNDLE_IDENTIFIER = dev.jmclaughlin.murmur' ios/Runner.xcodeproj/project.pbxproj | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `ios/Runner.xcodeproj/project.pbxproj` contains `IPHONEOS_DEPLOYMENT_TARGET = 17.0;` at least 3 times (Debug, Release, Profile)
    - `ios/Runner.xcodeproj/project.pbxproj` does NOT contain any `IPHONEOS_DEPLOYMENT_TARGET = 12.` / `13.` / `14.` / `15.` / `16.`
    - `ios/Podfile` contains `platform :ios, '17.0'` as a top-level line (not commented)
    - `ios/Podfile` contains `IPHONEOS_DEPLOYMENT_TARGET` inside the `post_install` block
    - `ios/Runner.xcodeproj/project.pbxproj` still contains `PRODUCT_BUNDLE_IDENTIFIER = dev.jmclaughlin.murmur` at least 3 times (bundle ID untouched by sed patches)
  </acceptance_criteria>
  <done>iOS 17.0 deployment target is set in all 3 authoritative locations; bundle ID is preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write Linux-compatible verify_ios_plist.sh</name>
  <files>scripts/verify_ios_plist.sh</files>
  <read_first>
    - ios/Runner/Info.plist (the file written in Task 1)
    - .planning/phases/01-scaffold-compliance-foundation/01-RESEARCH.md (§Validation Architecture §Phase Requirements → Test Map, FND-07 row)
  </read_first>
  <action>
Create `scripts/verify_ios_plist.sh` with EXACTLY the following. This script MUST work on both Linux (for local Phase 1 dev) and macOS (for Plan 09 CI) — so it uses grep only, not `plutil -p` (which is macOS-only):

```bash
#!/usr/bin/env bash
# Verifies Phase 1 ios/Runner/Info.plist contains every FND-07 compliance key.
# Works on Linux (no plutil) and macOS. Exit 0 on success, non-zero on violation.

set -euo pipefail

PLIST="ios/Runner/Info.plist"

if [ ! -f "$PLIST" ]; then
  echo "FAIL: $PLIST does not exist"
  exit 1
fi

fail=0

check_key_value() {
  local key="$1"
  local value_pattern="$2"
  local description="$3"
  # grep -A 1 for the key, then check the next line matches value_pattern
  if ! grep -A 1 "<key>${key}</key>" "$PLIST" | grep -q "$value_pattern"; then
    echo "FAIL: ${description} — expected <key>${key}</key> followed by ${value_pattern}"
    fail=1
  fi
}

check_key_present() {
  local key="$1"
  local description="$2"
  if ! grep -q "<key>${key}</key>" "$PLIST"; then
    echo "FAIL: ${description} — key <${key}> not present"
    fail=1
  fi
}

# CFBundleDisplayName = Murmur (D-02)
check_key_value "CFBundleDisplayName" "<string>Murmur</string>" "Display name must be 'Murmur'"

# Bundle identifier must remain $(PRODUCT_BUNDLE_IDENTIFIER) — NOT hard-coded
check_key_value "CFBundleIdentifier" '\$(PRODUCT_BUNDLE_IDENTIFIER)' "Bundle identifier must be \\\$(PRODUCT_BUNDLE_IDENTIFIER) variable"

# FND-07 keys
check_key_present "UIBackgroundModes" "FND-07: background audio mode"
if ! grep -A 2 "<key>UIBackgroundModes</key>" "$PLIST" | grep -q "<string>audio</string>"; then
  echo "FAIL: FND-07: UIBackgroundModes must contain <string>audio</string>"
  fail=1
fi

check_key_value "UIFileSharingEnabled" "<true/>" "FND-07: UIFileSharingEnabled must be true"
check_key_value "LSSupportsOpeningDocumentsInPlace" "<true/>" "FND-07: LSSupportsOpeningDocumentsInPlace must be true"

check_key_present "CFBundleDocumentTypes" "FND-07: EPUB document type"
if ! grep -q "org.idpf.epub-container" "$PLIST"; then
  echo "FAIL: FND-07: CFBundleDocumentTypes must reference org.idpf.epub-container"
  fail=1
fi

check_key_value "ITSAppUsesNonExemptEncryption" "<false/>" "FND-07: ITSAppUsesNonExemptEncryption must be false (no non-exempt crypto in Phase 1)"

if [ "$fail" -eq 0 ]; then
  echo "OK: ios/Runner/Info.plist has all Phase 1 FND-07 keys"
  exit 0
else
  exit 1
fi
```

Make executable: `chmod +x scripts/verify_ios_plist.sh`.

Run it:

```
bash scripts/verify_ios_plist.sh
```

Should print `OK: ios/Runner/Info.plist has all Phase 1 FND-07 keys` and exit 0.
  </action>
  <verify>
    <automated>test -x scripts/verify_ios_plist.sh && bash scripts/verify_ios_plist.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_ios_plist.sh` exists and is executable
    - `scripts/verify_ios_plist.sh` does NOT use `plutil` (so it works on Linux)
    - `bash scripts/verify_ios_plist.sh` exits 0 and prints `OK: ios/Runner/Info.plist has all Phase 1 FND-07 keys`
    - The script checks: `CFBundleDisplayName`, `CFBundleIdentifier`, `UIBackgroundModes`, `UIFileSharingEnabled`, `LSSupportsOpeningDocumentsInPlace`, `CFBundleDocumentTypes`, `org.idpf.epub-container`, `ITSAppUsesNonExemptEncryption`
  </acceptance_criteria>
  <done>Shell assertion script works on Linux (no plutil dependency) and passes against the Task 1 plist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Store export compliance declaration | `ITSAppUsesNonExemptEncryption = false` is a legally-meaningful statement in App Store Connect — must be truthful |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | V14 Configuration (Export compliance) | `ITSAppUsesNonExemptEncryption` key | mitigate | Task 1 sets this to `<false/>` with a comment explaining why (Phase 1 has no network; Phase 4 adds HTTPS model download which is exempt under EAR §740.17(b)). Script in Task 3 asserts the value. Phase 4 transition MUST re-verify this key |
| T-01-04 | V10 (least privilege) | Info.plist key over-declare | mitigate | Task 1 declares only FND-07 keys — no microphone (`NSMicrophoneUsageDescription`), no camera, no contacts, no location. If a future plan wants to add one, it must justify it against ASVS V10 |
</threat_model>

<verification>
- `bash scripts/verify_ios_plist.sh` passes on Linux
- `grep -c 'IPHONEOS_DEPLOYMENT_TARGET = 17.0;' ios/Runner.xcodeproj/project.pbxproj` returns at least 3
- `grep -c 'PRODUCT_BUNDLE_IDENTIFIER = dev.jmclaughlin.murmur' ios/Runner.xcodeproj/project.pbxproj` returns at least 3
- Plan 09's `ios-scaffold` CI job will additionally run this same script on `macos-14`
</verification>

<success_criteria>
FND-07 and FND-01 (iOS side) are satisfied by the files this plan produces. Plan 09's `ios-scaffold` CI job can run `flutter build ios --no-codesign` on `macos-14` and compile successfully against iOS 17.0 SDK.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-compliance-foundation/01-04-SUMMARY.md` with: actual Flutter 3.41 default IPHONEOS_DEPLOYMENT_TARGET recorded in Task 2 Step 1 (answers research Open Question 4), diff of project.pbxproj sed patches (count of lines changed), output of `bash scripts/verify_ios_plist.sh`.
</output>
