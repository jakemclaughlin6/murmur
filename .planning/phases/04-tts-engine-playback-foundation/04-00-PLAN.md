---
phase: 04
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pubspec.yaml
  - integration_test/tts_spike_test.dart
  - integration_test/DEVICE_CHECKLIST.md
  - lib/features/tts/spike/spike_page.dart
  - lib/app/router.dart
  - assets/kokoro/voices.bin
  - assets/kokoro/tokens.txt
  - assets/kokoro/espeak-ng-data/
  - android/app/src/main/AndroidManifest.xml
autonomous: false
requirements: [TTS-02, TTS-03, TTS-05]
must_haves:
  truths:
    - "On a physical Android device, a test harness synthesizes one sentence with sherpa_onnx 1.12.36 + Kokoro v0_19 and plays it through just_audio"
    - "The OfflineTtsKokoroModelConfig → OfflineTtsModelConfig → OfflineTtsConfig chain compiles and loads successfully"
    - "voices.bin, tokens.txt, espeak-ng-data/ are bundled as assets and copyable to getApplicationSupportDirectory()"
    - "A cancellation probe records whether sherpa_onnx.OfflineTts.generateWithConfig supports interruption (expected: NO per RESEARCH Pitfall 4)"
  artifacts:
    - path: "integration_test/tts_spike_test.dart"
      provides: "The one-sentence hear-it spike test"
    - path: "lib/features/tts/spike/spike_page.dart"
      provides: "Manual spike page mounted at /_spike/tts"
    - path: "integration_test/DEVICE_CHECKLIST.md"
      provides: "Physical-device manual verification script"
    - path: "assets/kokoro/voices.bin"
      provides: "Bundled Kokoro voice style vectors (~5.5MB)"
  key_links:
    - from: "spike_page.dart"
      to: "sherpa_onnx.OfflineTts.generateWithConfig"
      via: "OfflineTtsConfig built against app-support-copied assets"
---

<objective>
Wave 0 spike — prove "hear one sentence" on a physical Android device before any other Phase 4 work.

Purpose: De-risk the single highest-uncertainty piece of Phase 4 (sherpa_onnx 1.12.36 Flutter bindings + Kokoro v0_19 + just_audio) with a minimal harness. Per RESEARCH.md §Summary: every other Phase 4 decision depends on this working.
Output: Committed assets, a spike page reachable at `/_spike/tts`, an integration test, a device checklist, and a documented answer to the cancellation question (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tts-engine-playback-foundation/04-CONTEXT.md
@.planning/phases/04-tts-engine-playback-foundation/04-RESEARCH.md
@.planning/phases/04-tts-engine-playback-foundation/04-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Sherpa Kokoro config chain (verified from upstream flutter-examples/tts/model.dart). -->
Paths resolved via `getApplicationSupportDirectory()` — NOT documents dir (Pitfall 2).

```dart
final kokoro = sherpa_onnx.OfflineTtsKokoroModelConfig(
  model:   '$appSupport/kokoro-en-v0_19/model.int8.onnx',
  voices:  '$appSupport/kokoro-en-v0_19/voices.bin',
  tokens:  '$appSupport/kokoro-en-v0_19/tokens.txt',
  dataDir: '$appSupport/kokoro-en-v0_19/espeak-ng-data',
  lexicon: '',
);
final modelConfig = sherpa_onnx.OfflineTtsModelConfig(
  vits: sherpa_onnx.OfflineTtsVitsModelConfig(),
  kokoro: kokoro, numThreads: 2, debug: false, provider: 'cpu',
);
final tts = sherpa_onnx.OfflineTts(sherpa_onnx.OfflineTtsConfig(model: modelConfig));
final audio = tts.generateWithConfig(
  text: 'Welcome to murmur.',
  config: sherpa_onnx.OfflineTtsGenerationConfig(sid: 1, speed: 1.0),
);
// audio.samples : Float32List; audio.sampleRate : int (24000)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Phase 4 deps + bundle Kokoro static assets + verify Android FG-service manifest</name>
  <read_first>
    - pubspec.yaml (Phase 2 dependency_overrides section is load-bearing — do not remove)
    - CLAUDE.md §Risks #1, #6 and §Version Compatibility
    - .planning/phases/04-tts-engine-playback-foundation/04-RESEARCH.md §Versions to pin, §Kokoro Model — Asset Layout, §Pitfall 6
    - android/app/src/main/AndroidManifest.xml
    - .planning/phases/01-scaffold-compliance-foundation/01-08-SUMMARY.md (for how assets were previously declared)
  </read_first>
  <action>
    1) Add these to `pubspec.yaml` `dependencies:` (exact version for sherpa_onnx, caret for others):
       ```
       sherpa_onnx: 1.12.36          # EXACT pin — NOT caret. Patch cadence is fast (RESEARCH §Versions).
       just_audio: ^0.10.5
       audio_service: ^0.18.18
       audio_session: ^0.2.3
       http: ^1.2.0
       crypto: ^3.0.5
       ```
       Do NOT add `connectivity_plus` (D-02 honor-system).
       Do NOT add `dio` (CLAUDE.md rejects).

    2) Obtain the Kokoro v0_19 static assets (voices.bin ~5.5MB, tokens.txt ~1KB, espeak-ng-data/ ~1MB, ~50 files) by downloading `kokoro-int8-en-v0_19.tar.bz2` from
       `https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/kokoro-int8-en-v0_19.tar.bz2`
       using `curl -L -o /tmp/kokoro.tar.bz2 <url>` then extract with `tar -xjf /tmp/kokoro.tar.bz2 -C /tmp/`.
       Copy the following into the repo (commit them — they are static build-time assets per D-06/TTS-03):
       - `/tmp/kokoro-en-v0_19/voices.bin` → `assets/kokoro/voices.bin`
       - `/tmp/kokoro-en-v0_19/tokens.txt` → `assets/kokoro/tokens.txt`
       - `/tmp/kokoro-en-v0_19/espeak-ng-data/` (whole dir) → `assets/kokoro/espeak-ng-data/`
       - `/tmp/kokoro-en-v0_19/LICENSE` → `assets/kokoro/LICENSE`
       Do NOT copy `model.int8.onnx` — it is downloaded at runtime (TTS-02), never bundled.

    3) Update `pubspec.yaml` `flutter: assets:` to include the bundled Kokoro tree. Add:
       ```
         - assets/kokoro/voices.bin
         - assets/kokoro/tokens.txt
         - assets/kokoro/LICENSE
         - assets/kokoro/espeak-ng-data/
       ```
       Flutter's asset system recurses directories when listed with a trailing slash. Verify during build.

    4) Verify/augment `android/app/src/main/AndroidManifest.xml`:
       - Must contain `<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />`
       - Must contain `<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />` (Android 14+ gate, Pitfall 6; Phase 1 FND-08 should have added — verify, add if missing).
       - Must contain `<uses-permission android:name="android.permission.INTERNET" />` (already there from Phase 1).
       If any missing, add. Do not remove anything.

    5) Run `flutter pub get` and then `flutter pub deps --style=compact | grep sherpa_onnx` to confirm 1.12.36 is resolved exactly.

    Notes:
      - iOS Info.plist `UIBackgroundModes: audio` already landed Phase 1 FND-07. Do NOT re-add.
      - The single point of this task: compile green with new deps + assets resolvable.
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; flutter analyze &amp;&amp; flutter pub deps --style=compact 2&gt;&amp;1 | grep -E '^[├└|]\s*sherpa_onnx 1\.12\.36$' &amp;&amp; test -f assets/kokoro/voices.bin &amp;&amp; test -f assets/kokoro/tokens.txt &amp;&amp; test -d assets/kokoro/espeak-ng-data &amp;&amp; grep -q FOREGROUND_SERVICE_MEDIA_PLAYBACK android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <done>
    Dependencies resolve, sherpa_onnx pinned at exactly 1.12.36, Kokoro static assets committed under `assets/kokoro/`, Android manifest has `FOREGROUND_SERVICE_MEDIA_PLAYBACK`, `flutter analyze` passes with no errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build spike page, integration test, and cancellation probe</name>
  <files>
    lib/features/tts/spike/spike_page.dart,
    lib/features/tts/spike/copy_assets.dart,
    integration_test/tts_spike_test.dart,
    lib/app/router.dart
  </files>
  <read_first>
    - lib/app/router.dart (existing go_router setup)
    - lib/main.dart
    - .planning/phases/04-tts-engine-playback-foundation/04-RESEARCH.md §Patterns 3, 4, §Pitfall 1, §Pitfall 4
    - pub.dev docs for sherpa_onnx 1.12.36 (OfflineTtsKokoroModelConfig, OfflineTts, OfflineTtsGenerationConfig)
  </read_first>
  <behavior>
    - Test 1: `copyAssetsToSupportDir()` copies `assets/kokoro/voices.bin`, `tokens.txt`, and `espeak-ng-data/*` into `getApplicationSupportDirectory()/kokoro-en-v0_19/` idempotently.
    - Test 2 (integration, device-only): Given `model.int8.onnx` already present (test pre-populates by downloading or reading from a fixture path set via `--dart-define=MODEL_FIXTURE_PATH=...`), the spike page synthesizes "Welcome to murmur." with `sid=1` at `speed=1.0` and produces a non-empty `Float32List` with sampleRate 24000.
    - Test 3 (cancellation probe): Immediately after kicking off `generateWithConfig` in an isolate for a 40-word sentence, the UI attempts any available cancel API; test records to stdout whether cancellation interrupted synthesis or the full sentence completed. **Expected per RESEARCH Pitfall 4: no cancellation; full sentence completes.**
  </behavior>
  <action>
    1) Create `lib/features/tts/spike/copy_assets.dart` exporting:
       ```dart
       /// Copy bundled Kokoro assets from rootBundle to [getApplicationSupportDirectory]/kokoro-en-v0_19/.
       /// Idempotent: if target files already exist with matching size, skip.
       /// Does NOT copy model.int8.onnx — that is downloaded at runtime (TTS-02).
       Future<String> copyKokoroAssetsToSupportDir();  // returns absolute path to kokoro-en-v0_19/ dir
       ```
       Implementation:
       - List of asset paths: `assets/kokoro/voices.bin`, `assets/kokoro/tokens.txt`, `assets/kokoro/LICENSE`, plus every file under `assets/kokoro/espeak-ng-data/` (read `AssetManifest.bin` via `rootBundle.loadString('AssetManifest.json')` and filter to paths starting with `assets/kokoro/espeak-ng-data/`).
       - For each asset: `final bytes = await rootBundle.load(path); await File(dest).writeAsBytes(bytes.buffer.asUint8List());`
       - Create subdir `espeak-ng-data/` before writing files into it.

    2) Create `lib/features/tts/spike/spike_page.dart` — a StatefulWidget with:
       - "Copy assets" button → calls `copyKokoroAssetsToSupportDir()`, shows resolved path.
       - "Pick model.int8.onnx" button → uses file_picker (already a dep) to let Jake manually place the ~80MB model file into `$support/kokoro-en-v0_19/model.int8.onnx` for spike purposes. DO NOT implement download here — that is 04-02's job.
       - "Synthesize + play" button → constructs the full sherpa_onnx config chain per RESEARCH Pattern 3, calls `tts.generateWithConfig(text: 'Welcome to murmur. This is how I sound reading your books.', config: OfflineTtsGenerationConfig(sid: 1, speed: 1.0))`, wraps the float32 PCM into a WAV via an inline 44-byte-header helper (do NOT create `lib/features/tts/audio/wav_wrap.dart` yet — that lives in 04-03), writes to a temp file, plays via `just_audio.AudioPlayer.setAudioSource(AudioSource.file(...))` and `play()`.
       - "Cancel probe" button → kicks off a long-sentence (≥40 words) synth in an isolate via `Isolate.run`, waits 50ms, then tries every plausible cancel mechanism the sherpa_onnx 1.12.36 API surfaces (check the public API for any `.free()`, `.cancel()`, disposal of OfflineTts mid-generate, etc.) and logs the result to a `Text` widget. Expected result: NO cancellation hook exists; log `"No cancellation primitive found — D-12 fallback path confirmed."`
       - Shows current state (idle / copying / synthesizing / playing / error) + any error message.

    3) Register the spike route in `lib/app/router.dart` under path `/_spike/tts` (underscore-prefix to signal internal). Only available in debug builds: guard with `if (kDebugMode)` around the route entry so it's stripped from release.

    4) Create `integration_test/tts_spike_test.dart` that:
       - Calls `IntegrationTestWidgetsFlutterBinding.ensureInitialized()`.
       - Pumps the spike page.
       - Runs Test 1 automatically (asset copy). Asserts files exist under support dir.
       - Runs Test 2 GUARDED by `const String.fromEnvironment('MODEL_FIXTURE_PATH')` — only runs if caller pre-placed the model; otherwise `markTestSkipped`. This keeps CI green without an 80MB artifact.
       - Runs Test 3 (cancellation probe) unconditionally after Test 2 succeeds; records result in a `test_report.txt` file written under `getApplicationSupportDirectory()` so the device checklist can scrape it.

    5) Pattern-match the spike page output on screen with large, legible text so Jake can verify audibly + visually on a phone.

    Constraints:
      - This task does NOT implement the full isolate protocol (D-13) — that's 04-04. Use `Isolate.run` for the cancellation probe only.
      - No worker isolate lifecycle, no playbackStateProvider, no cache.
      - Absolutely NO code under `lib/features/reader/`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/tts/spike/ integration_test/tts_spike_test.dart</automated>
  </verify>
  <done>
    `flutter analyze` passes for spike files. Integration test compiles. Spike page reachable at `/_spike/tts` in debug builds. Cancellation probe implemented and its output format is deterministic (scrapeable string).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device verification — hear one sentence on physical Android</name>
  <action>Prepare the device checklist (see how-to-verify below) and pause execution until Jake runs every step on a physical Android device and reports results. No automated actions here — this is a blocking gate.</action>
  <what-built>
    Phase 4 dependencies pinned, Kokoro static assets bundled, spike page at `/_spike/tts`, and an integration test that copies assets and probes cancellation. This checkpoint is the gate: nothing in Phase 4 proceeds until Jake has heard a Kokoro sentence from a physical Android device.
  </what-built>
  <how-to-verify>
    Write `integration_test/DEVICE_CHECKLIST.md` before this checkpoint (as part of Task 2 deliverables if not already done — add if missing) containing:

    1. **Build** — `flutter build apk --debug`
    2. **Install** — `adb install -r build/app/outputs/flutter-apk/app-debug.apk`
    3. **Place model** — manually `adb push ~/Downloads/model.int8.onnx /sdcard/murmur-spike/` and in the app tap "Pick model.int8.onnx" to select it (the spike copies into app support dir).
    4. **Navigate** — launch app, deep-link or add a debug-build-only button on the Library screen that navigates to `/_spike/tts`. (Acceptable to navigate via `adb shell am start -a android.intent.action.VIEW -d "murmur://_spike/tts"` if easier — planner discretion.)
    5. **Copy assets** — tap "Copy assets", confirm success message.
    6. **Synthesize + play** — tap "Synthesize + play" with voice sid=1 (af_bella) and speed=1.0. Expected: audible sentence "Welcome to murmur. This is how I sound reading your books." within ~1–2 seconds on a mid-range phone.
    7. **Speed probe** — repeat with speed=2.0. Note whether pitch is preserved (expected on modern Android per Pitfall 5) or distorted. Record finding in device checklist.
    8. **Cancellation probe** — tap "Cancel probe". Read output: expected `"No cancellation primitive found — D-12 fallback path confirmed."` If a cancellation primitive IS found, STOP and update D-12 + 04-04 plan before proceeding.
    9. **iOS deferral** — no iOS physical-device verification this wave (no-Mac constraint); iOS gets CI-only (`workflow_dispatch xcarchive`) until Phase 4 exit.

    Fill in checklist with: pass/fail for each step, recorded latency (rough stopwatch), pitch-preservation observation, cancellation probe literal output.
  </how-to-verify>
  <resume-signal>Type "spike pass" with the recorded observations inline, or describe the failure to block Phase 4 planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → app | Single model download from k2-fsa GitHub Releases (not in this plan; scoped here is asset integrity only) |
| rootBundle → app support dir | Copying bundled Kokoro assets (trusted bytes from the APK/IPA) |
| UI isolate → sherpa_onnx native layer | Text string passed to `generateWithConfig` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | Bundled Kokoro assets (voices.bin, tokens.txt) | accept | Shipped inside the signed APK/IPA; tamper-resistant at distribution. No runtime hash needed. |
| T-04-00-02 | Information Disclosure | Spike page exposed in release builds | mitigate | Guard `/_spike/tts` route entry with `kDebugMode`; route is absent from release binary. |
| T-04-00-03 | Denial of Service | Spike synth of attacker-controlled text | accept | Spike page text is hard-coded; user cannot inject. |
| T-04-00-04 | Tampering | Asset copy path-traversal via espeak-ng-data filenames | mitigate | Only `AssetManifest.json`-listed paths are copied; filenames are known-good at build time; no user input reaches the path. |
</threat_model>

<verification>
- `flutter pub get` resolves with sherpa_onnx exactly at 1.12.36.
- `flutter analyze` is clean on all added files.
- Bundled assets under `assets/kokoro/` committed to git and flutter-assets-listed.
- Spike page reachable in debug builds only.
- Device checklist signed off by Jake ("spike pass").
- Cancellation probe output documented and incorporated into 04-04 planning decisions.
</verification>

<success_criteria>
- Jake hears "Welcome to murmur. This is how I sound reading your books." in voice sid=1 from a physical Android device.
- D-12 cancellation question answered empirically (expected: fallback path = "let current finish, discard result").
- All later plans have a green light to proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tts-engine-playback-foundation/04-00-SUMMARY.md` documenting:
- Kokoro asset SHA-256 fingerprints (for reproducibility)
- Cancellation probe literal output
- On-device 1.0× and 2.0× playback observations (pitch preservation)
- Any surprise errors encountered and fixes applied
- Confirmation that Phase 4 Wave 1+ is unblocked
</output>
