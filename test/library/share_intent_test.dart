/// Wave 0 stub — Share / Open-in intent handling.
///
/// Covers LIB-02 per 02-VALIDATION.md. D-14: Share/Open-in routes through
/// the same import pipeline as the file picker. Uses
/// receive_sharing_intent ^1.8.1 (substitution established in Plan 02-01,
/// see spike-notes.md). Real tests land in Plan 02-05.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Share intent', () {
    test(
      'TODO: Share/Open-in EPUB URI flows through the same import pipeline as file picker',
      () {},
      skip: 'Wave 0 stub — implemented by Plan 05',
    );
  });
}
