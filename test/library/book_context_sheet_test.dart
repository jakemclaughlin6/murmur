/// Wave 0 stub — book context bottom sheet.
///
/// Covers LIB-09 per 02-VALIDATION.md. D-17: long-press opens a modal
/// bottom sheet with Book Info + Delete (with confirm dialog). Real tests
/// land in Plan 02-07.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Book context sheet', () {
    test(
      'TODO: long-press opens bottom sheet with Book Info and Delete actions',
      () {},
      skip: 'Wave 0 stub — implemented by Plan 07',
    );
  });
}
