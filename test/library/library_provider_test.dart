/// Wave 0 stub — library provider (sort chips).
///
/// Covers LIB-07 per 02-VALIDATION.md. Sort modes: Recently read | Title |
/// Author. Riverpod provider derives the ordered list from the Drift books
/// stream. Real tests land in Plan 02-06.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Library provider sort', () {
    test(
      'TODO: sorts library by Recently read / Title / Author per selected chip',
      () {},
      skip: 'Wave 0 stub — implemented by Plan 06',
    );
  });
}
