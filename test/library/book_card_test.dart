/// Wave 0 stub — BookCard widget.
///
/// Covers LIB-06 per 02-VALIDATION.md. D-07: full-bleed BoxFit.cover art;
/// D-08: missing-cover fallback (ClayColors.background + menu_book_outlined
/// icon); D-09: title + author two-line text area; D-10: progress ring only
/// when reading_progress_chapter is non-null. Real tests land in Plan 02-06.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('BookCard widget', () {
    test(
      'TODO: renders cover art full-bleed, title and author, optional progress ring',
      () {},
      skip: 'Wave 0 stub — implemented by Plan 06',
    );
  });
}
