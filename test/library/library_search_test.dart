/// Wave 0 stub — library search field.
///
/// Covers LIB-08 per 02-VALIDATION.md. Debounced search over title +
/// author; recommended debounce 300ms (CONTEXT Claude's discretion). Real
/// tests land in Plan 02-07.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Library search', () {
    test(
      'TODO: debounced search filters library grid on title and author substring',
      () {},
      skip: 'Wave 0 stub — implemented by Plan 07',
    );
  });
}
