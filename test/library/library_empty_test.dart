/// Wave 0 stub — library empty state.
///
/// Covers LIB-10 per 02-VALIDATION.md. D-18: reuses the Phase 1 placeholder
/// (96px menu_book_outlined icon, "Your library is empty" headline, "Import
/// an EPUB to start listening." body, FilledButton.icon). Plan 02 wires
/// that button to file_picker. Real tests land in Plan 02-07.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Library empty state', () {
    test(
      'TODO: empty library shows placeholder icon, headline, body, and wired Import button',
      () {},
      skip: 'Wave 0 stub — implemented by Plan 07',
    );
  });
}
