/// Wave 0 stub — EPUB parser DOM walker.
///
/// Covers LIB-03 per 02-VALIDATION.md. The parser will walk chapter XHTML
/// with `package:html` and emit `List<Block>` per D-01. Real tests land in
/// Plan 02-04 once the parser exists.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('EPUB parser DOM walker', () {
    test(
      'TODO: walks chapter XHTML and emits List<Block> per D-01',
      () {},
      skip: 'Wave 0 stub — implemented by Plan 04',
    );
  });
}
